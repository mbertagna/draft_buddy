This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
data/
  merged_player_data_cleaned.csv
build.sh
config.py
data_utils.py
Dockerfile
fantasy_draft_env.py
policy_network.py
reinforce_agent.py
requirements.txt
run.sh
simulate.py
train.py
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="data/merged_player_data_cleaned.csv">
player_id,name,position,projected_points,adp
0033280,Christian McCaffrey,RB,24.04,1.0
0036358,CeeDee Lamb,WR,22.010810810810813,2.6
0033040,Tyreek Hill,WR,22.834285714285716,3.2
0038542,Bijan Robinson,RB,15.01764705882353,5.0
0038120,Breece Hall,RB,17.525000000000002,5.4
0036963,Amon-Ra St. Brown,WR,19.888571428571428,6.2
0036900,Ja'Marr Chase,WR,18.916774193548385,6.6
0036322,Justin Jefferson,WR,22.58857142857143,7.0
0034844,Saquon Barkley,RB,17.509375000000002,9.2
0035676,A.J. Brown,WR,17.805405405405406,10.2
0036223,Jonathan Taylor,RB,14.847619047619048,10.4
0037740,Garrett Wilson,WR,13.055882352941175,12.4
0039139,Jahmyr Gibbs,RB,16.605555555555554,12.8
0039075,Puka Nacua,WR,19.744444444444444,14.2
0037840,Kyren Williams,RB,14.833333333333334,16.0
0039849,Marvin Harrison,WR,17.111999999999995,16.8
0036973,Travis Etienne,RB,14.988888888888887,17.4
0032764,Derrick Henry,RB,18.103636363636365,18.0
0037197,Isiah Pacheco,RB,12.221052631578948,20.2
0031381,Davante Adams,WR,18.644117647058824,22.0
0034857,Josh Allen,QB,28.85297297297297,24.2
0030506,Travis Kelce,TE,18.6,25.2
0037238,Drake London,WR,10.969090909090909,25.6
0037239,Chris Olave,WR,14.629032258064516,26.2
0039040,De'Von Achane,RB,17.641666666666666,27.4
0035700,Josh Jacobs,RB,17.88,28.2
0036554,Nico Collins,WR,15.04814814814815,28.2
0033873,Patrick Mahomes,QB,26.047000000000004,29.0
0037248,James Cook,RB,10.356756756756756,31.0
0031408,Mike Evans,WR,16.680000000000003,31.2
0036389,Jalen Hurts,QB,26.328333333333333,31.2
0037256,Rachaad White,RB,12.702777777777778,31.6
0039065,Sam LaPorta,TE,14.345,31.8
0035719,Deebo Samuel Sr.,WR,14.638235294117647,32.0
0036261,Brandon Aiyuk,WR,14.046153846153844,34.0
0036252,Michael Pittman,WR,15.115625,34.6
0033908,Cooper Kupp,WR,18.204545454545453,34.8
0034796,Lamar Jackson,QB,24.332,37.2
0033897,Joe Mixon,RB,16.200000000000003,38.6
0033906,Alvin Kamara,RB,16.09642857142857,39.6
0035640,DK Metcalf,WR,14.876470588235296,41.2
0038134,Kenneth Walker III,RB,13.983870967741936,41.4
0036613,Jaylen Waddle,WR,15.12121212121212,42.6
0031588,Stefon Diggs,WR,18.1972972972973,43.0
0034827,DJ Moore,WR,15.076470588235296,43.6
0036912,DeVonta Smith,WR,15.47837837837838,44.0
0034753,Mark Andrews,TE,12.988888888888887,45.8
0037744,Trey McBride,TE,9.576923076923077,46.4
0039337,Malik Nabers,WR,15.625865624660802,48.6
0039163,C.J. Stroud,QB,21.99882352941177,48.6
0038933,Dalton Kincaid,TE,9.705555555555556,53.6
0033553,James Conner,RB,16.026923076923076,56.0
0033288,George Kittle,TE,12.694594594594594,56.6
0031544,Amari Cooper,WR,15.633333333333333,56.8
0039164,Anthony Richardson,QB,19.67,57.6
0033293,Aaron Jones,RB,15.406666666666668,59.2
0037247,George Pickens,WR,11.508571428571427,59.6
0036275,D'Andre Swift,RB,13.225806451612904,60.6
0036442,Joe Burrow,QB,23.50689655172414,61.8
0035685,David Montgomery,RB,13.263636363636364,61.8
0036410,Tee Higgins,WR,14.217241379310344,62.2
0039064,Zay Flowers,WR,13.411111111111111,63.2
0036970,Kyle Pitts,TE,7.885185185185185,64.2
0038977,Tank Dell,WR,17.4,65.0
0033881,Evan Engram,TE,12.322222222222225,65.0
0038040,Zamir White,RB,3.6208333333333336,66.6
0036875,Rhamondre Stevenson,RB,13.820689655172414,66.6
0039067,Rashee Rice,WR,14.01,67.6
0033077,Dak Prescott,QB,24.284375,68.4
0035228,Kyler Murray,QB,21.15157894736842,70.2
0036893,Najee Harris,RB,12.433142857142858,70.8
0036264,Jordan Love,QB,19.460869565217394,71.4
0035659,Terry McLaurin,WR,13.15294117647059,71.6
0034837,Calvin Ridley,WR,14.229411764705883,74.4
0031687,Raheem Mostert,RB,14.125,74.6
0034775,Christian Kirk,WR,14.54709677419355,74.6
0033921,Chris Godwin,WR,13.857142857142858,76.2
0030279,Keenan Allen,WR,20.040000000000003,79.4
0036997,Javonte Williams,RB,11.06,81.2
0038041,Jake Ferguson,TE,8.906666666666666,81.6
0035261,Tony Pollard,RB,14.586111111111112,81.8
0033885,David Njoku,TE,11.790322580645162,84.2
0039146,Jayden Reed,WR,12.7,87.2
0035216,Diontae Johnson,WR,11.23225806451613,88.2
0039877,Xavier Worthy,WR,13.522817544688555,90.0
0033699,Austin Ekeler,RB,18.29375,91.0
0037834,Brock Purdy,QB,19.528387096774193,91.8
0037746,Brian Robinson,RB,11.733333333333334,92.6
0037228,Jaylen Warren,RB,8.914705882352942,92.6
0035250,Devin Singletary,RB,10.89837837837838,94.4
0036251,Zack Moss,RB,9.003703703703703,98.0
0039861,Rome Odunze,WR,15.118304125593282,98.4
0038543,Jaxon Smith-Njigba,WR,8.811764705882354,99.8
0039032,Tyjae Spears,RB,9.047058823529412,100.2
0039338,Brock Bowers,TE,11.816917562724012,100.6
0034791,Nick Chubb,RB,17.289473684210527,100.8
0039344,Jonathon Brooks,RB,17.454399671052634,101.4
0038124,Christian Watson,WR,11.136,102.2
0039871,Jayden Daniels,QB,23.971471788439803,102.6
0039867,Keon Coleman,WR,12.94954642097803,104.6
0039868,Ladd McConkey,WR,12.642065404475046,105.4
0030564,DeAndre Hopkins,WR,15.011538461538462,107.6
0029597,Justin Tucker,K,9.16216216216216,108.4
0039869,Caleb Williams,QB,24.20662715517241,109.0
0034351,Dallas Goedert,TE,11.056666666666668,109.6
0035662,Marquise Brown,WR,11.748,110.2
0038994,Jordan Addison,WR,13.370588235294118,110.4
0038597,Chase Brown,RB,5.35,111.2
0033303,Harrison Butker,K,8.837837837837839,112.2
0034348,Courtland Sutton,WR,11.374193548387098,112.4
0037267,Jerome Ford,RB,11.38,113.0
0033045,Ezekiel Elliott,RB,10.888235294117647,113.2
0037692,Brandon Aubrey,K,10.055555555555555,113.4
0034184,Gus Edwards,RB,9.03448275862069,113.6
0036212,Tua Tagovailoa,QB,21.023870967741935,115.8
0033106,Jared Goff,QB,20.78918918918919,116.0
0039862,Brian Thomas,WR,14.044063021316031,116.2
0037240,Jameson Williams,WR,5.976190476190476,120.2
0034383,Dalton Schultz,TE,10.541176470588235,125.6
0035229,T.J. Hockenson,TE,14.22121212121212,126.4
0039738,Blake Corum,RB,10.4406162464986,128.0
0036971,Trevor Lawrence,QB,20.221714285714285,129.4
0036355,Justin Herbert,QB,20.759354838709676,130.8
0037261,Khalil Shakir,WR,6.5600000000000005,132.0
0036555,Chuba Hubbard,RB,9.593103448275862,132.4
0036158,J.K. Dobbins,RB,11.94,132.4
0039866,Trey Benson,RB,13.743321142459076,133.6
0036894,Pat Freiermuth,TE,8.911111111111111,135.4
0023459,Aaron Rodgers,QB,16.177777777777777,136.2
0032211,Tyler Lockett,WR,13.547058823529412,139.4
0033702,Younghoe Koo,K,8.205882352941176,140.0
0036290,Cole Kmet,TE,10.35625,141.0
0037816,Romeo Doubs,WR,9.95,142.0
0029604,Kirk Cousins,QB,22.58230769230769,142.6
0033282,Curtis Samuel,WR,10.196969696969695,143.6
0034960,Jakobi Meyers,WR,13.596,143.6
0036407,Jerry Jeudy,WR,11.451612903225806,143.8
0033787,Jake Elliott,K,8.108108108108109,145.4
0039165,Zach Charbonnet,RB,6.63125,147.4
0032726,Ka'imi Fairbairn,K,8.258064516129032,149.0
0036854,Evan McPherson,K,7.722222222222222,149.2
0033536,Mike Williams,WR,15.10625,149.8
0026498,Matthew Stafford,QB,18.4064,150.6
0036139,Rico Dowdle,RB,5.764705882352941,150.8
0036988,Josh Palmer,WR,10.855555555555556,151.4
0038562,Jake Moody,K,7.9,156.6
0037263,Tyler Allgeier,RB,9.181818181818182,156.8
0037276,Ty Chandler,RB,7.571428571428571,160.8
0030035,Adam Thielen,WR,12.228571428571428,161.0
0038794,Jaleel McLaughlin,RB,6.235294117647059,162.0
0033537,Deshaun Watson,QB,16.698333333333334,163.8
0034855,Baker Mayfield,QB,17.549032258064514,166.6
0037224,Cameron Dicker,K,8.827586206896552,166.8
0036196,Gabe Davis,WR,11.415151515151514,168.8
0031236,Brandin Cooks,WR,11.203333333333337,169.2
0039875,Jaylen Wright,RB,6.715286195286195,169.8
0039144,Luke Musgrave,TE,7.215384615384616,169.8
0039858,Adonai Mitchell,WR,9.918112449799196,170.6
0036328,Antonio Gibson,RB,9.461290322580645,173.8
0039342,Xavier Legette,WR,13.10686746987952,178.6
0037838,Isaiah Likely,TE,7.255172413793104,180.2
0034270,Tyler Conklin,TE,7.488235294117647,182.0
0038393,Dontayvion Wicks,WR,7.66235294117647,182.2
0036309,Darnell Mooney,WR,7.216,183.2
0036162,Tyler Bass,K,7.918918918918919,183.4
0039859,Ja'Lynn Polk,WR,11.74147505577867,184.2
0039732,Bo Nix,QB,21.849248656839784,184.6
0037545,Rashid Shaheed,WR,9.81153846153846,187.6
0039361,Bucky Irving,RB,6.229785247432307,191.0
0036906,Khalil Herbert,RB,9.692,192.2
0030565,Geno Smith,QB,20.244242424242422,192.4
0039152,Will Levis,QB,13.335555555555556,195.4
0031492,Jason Myers,K,8.942857142857143,196.4
0034794,Jason Sanders,K,7.888888888888889,199.4
0038621,Demario Douglas,WR,7.6571428571428575,203.0
0031280,Derek Carr,QB,17.94375,218.0
0030061,Zach Ertz,TE,9.841176470588236,224.6
0033357,Taysom Hill,QB,9.3225,164.5
0029621,Greg Zuerlein,K,8.774193548387096,181.8
0039811,MarShawn Lloyd,RB,9.650793103448276,182.0
0037525,Jordan Mason,RB,4.316666666666666,187.5
0033090,Hunter Henry,TE,7.717241379310345,193.0
0039794,Braelon Allen,RB,5.728518518518519,198.5
0038997,Josh Downs,WR,9.41764705882353,200.5
0034161,Daniel Carlson,K,8.65625,200.8
0035269,Matt Gay,K,8.205882352941176,201.0
0031203,Cairo Santos,K,7.666666666666667,202.0
0039860,Ray Davis,RB,6.150641503572538,205.5
0039150,Bryce Young,QB,11.27375,216.5
0039384,Tyrone Tracy,RB,3.615162037037037,217.0
0036945,Justin Fields,QB,21.648571428571422,221.0
0038559,Michael Wilson,WR,8.807692307692308,226.3
0029263,Russell Wilson,QB,19.08866666666667,226.3
0033858,Jonnu Smith,TE,6.637037037037037,227.3
0036919,Kenneth Gainwell,RB,6.167027027027027,229.3
0036360,Clyde Edwards-Helaire,RB,6.082758620689655,231.5
0034829,Mike Gesicki,TE,5.245454545454545,236.5
0034869,Sam Darnold,QB,8.10625,249.0
0038129,Cade Otton,TE,7.690909090909091,196.0
0037741,Jahan Dotson,WR,9.013793103448275,201.0
0039391,Kimani Vidal,RB,2.98,207.3
0035689,Dawson Knox,TE,7.658620689655172,212.3
0038544,Quentin Johnston,WR,5.529411764705882,212.7
0037258,Dameon Pierce,RB,9.225925925925926,215.0
0039810,Jermaine Burton,WR,7.574530120481928,215.3
0039864,Ben Sinnott,TE,7.649926680972818,215.7
0039739,Roman Wilson,WR,7.272472289156624,215.7
0039404,Will Reichard,K,9.22876447876448,220.3
0039325,Carson Steele,RB,3.32198275862069,225.7
0039851,Drake Maye,QB,23.736316421707176,226.7
0034972,Alexander Mattison,RB,6.552941176470589,228.7
0038905,Blake Grupe,K,8.411764705882353,231.0
0036550,Rashod Bateman,WR,6.134782608695652,232.0
0039355,Luke McCaffrey,WR,6.33663380795911,233.7
0038976,Marvin Mims Jr.,WR,4.78,233.7
0038555,Tank Bigsby,RB,1.907692307692308,235.0
0034975,Justice Hill,RB,5.365625,236.7
0039409,Cam Little,K,6.225378787878788,236.7
0038117,Wan'Dale Robinson,WR,8.942857142857143,237.3
0039857,Troy Franklin,WR,6.211180722891567,239.0
0039021,Roschon Johnson,RB,6.806666666666667,244.3
0035710,Daniel Jones,QB,17.858333333333334,246.7
0035644,Noah Fant,TE,6.190625,248.0
0039356,Ja'Tavion Sanders,TE,4.690660330302184,249.0
0033009,Tyler Boyd,WR,9.328333333333331,250.7
0033948,Jamaal Williams,RB,9.756666666666666,253.7
0038454,Keaton Mitchell,RB,10.414285714285716,254.3
0039373,Audric Estime,RB,4.703819444444444,257.0
0036244,Colby Parkinson,TE,4.665517241379311,257.7
0039746,Will Shipley,RB,6.176370656370656,258.7
0037252,Greg Dulcich,TE,7.633333333333334,259.0
0036040,Juwan Johnson,TE,8.020689655172413,262.3
0037666,Jalen Tolbert,WR,4.0,263.7
0035500,Greg Dortch,WR,9.13,265.3
0032775,Demarcus Robinson,WR,8.25,266.7
0035535,Darius Slayton,WR,9.278125,270.0
0033526,Samaje Perine,RB,8.047222222222222,271.3
0039066,Michael Mayer,TE,5.95,271.7
0030098,Dustin Hopkins,K,9.15,159.5
0036913,Kadarius Toney,WR,5.268000000000001,282.7
0031136,Chris Boswell,K,7.533333333333333,186.5
0039856,Ricky Pearsall,WR,13.228986534372783,187.5
0039415,Brenden Rice,WR,1.7000000000000002,293.3
0039792,Devontez Walker,WR,5.907707831325301,294.3
0039870,Malachi Corley,WR,8.809036689745408,199.5
0039863,Michael Penix,QB,22.455567279875385,302.3
0038396,Eric Gray,RB,2.6,306.0
0039751,Jordan Whittington,WR,2.2615593803786576,307.7
0031610,Darren Waller,TE,9.9,213.5
0039407,Dylan Laube,RB,1.8930995475113124,310.0
0029822,Brandon McManus,K,7.852941176470588,311.3
0036984,Trey Sermon,RB,2.466666666666667,316.0
0031235,Odell Beckham Jr.,WR,7.36875,225.0
0026858,Graham Gano,K,7.222222222222222,227.0
0032569,Wil Lutz,K,7.0606060606060606,230.0
0039853,Javon Baker,WR,6.033434187879338,323.7
0036637,Noah Gray,TE,4.0475,239.5
0038551,Kendre Miller,RB,6.185714285714285,243.5
0033857,JuJu Smith-Schuster,WR,9.04,245.5
0039876,Malik Washington,WR,3.45233505450373,331.0
0038115,Daniel Bellinger,TE,5.253846153846154,331.7
0035289,Gardner Minshew,QB,12.582727272727272,253.0
0039062,Jonathan Mingo,WR,5.653333333333333,340.3
0034386,Justin Watson,WR,4.434210526315789,264.0
0038996,Tucker Kraft,TE,7.658333333333334,264.0
0034419,Braxton Berrios,WR,4.05448275862069,267.0
0039750,Joshua Karty,K,7.635416666666667,269.5
0035243,Miles Sanders,RB,9.483333333333334,270.5
0039228,Louis Rees-Zammit,RB,3.32198275862069,270.5
0039855,Jalen McMillan,WR,6.662908184461983,275.5
0037801,KaVontae Turpin,WR,3.6166666666666663,279.0
0036259,Jauan Jennings,WR,5.413333333333333,280.0
0038090,Skyy Moore,WR,3.8833333333333337,280.5
0038752,Jake Bobo,WR,4.064285714285715,281.0
0038938,Jalin Hyatt,WR,4.869230769230769,282.5
0034830,Hayden Hurst,TE,7.156000000000001,287.0
0038402,Anders Carlson,K,6.631578947368421,287.5
0035192,Joey Slye,K,6.151515151515151,288.0
0036626,Dyami Brown,WR,3.5380000000000003,290.5
0038102,Kenny Pickett,QB,11.5224,290.5
0034798,Durham Smythe,TE,4.4,292.0
0036096,J.J. Taylor,RB,2.7,293.0
0038563,Tre Tucker,WR,5.523076923076923,293.5
0038579,Aidan O'Connell,QB,13.62,295.0
0038622,Deuce Vaughn,RB,2.5,295.5
0038619,Andrei Iosivas,WR,5.622222222222223,298.5
0036980,Elijah Moore,WR,7.206451612903226,301.0
0033925,D'Onta Foreman,RB,9.488,302.0
0034521,Allen Lazard,WR,8.496428571428572,303.0
0038558,Darnell Washington,TE,1.4555555555555555,305.0
0033949,Joshua Dobbs,QB,16.9,307.0
0033110,Tyler Higbee,TE,8.140625,371.7
0038547,Luke Schoonmaker,TE,2.35,308.5
0038097,Cade York,K,6.705882352941177,309.5
0039363,Isaac Guerendo,RB,6.092121776949363,310.0
0039359,Cade Stover,TE,4.016258941344779,313.5
0038612,A.T. Perry,WR,7.575,314.0
0033891,Zay Jones,WR,11.737037037037036,314.5
0032950,Carson Wentz,QB,18.991111111111113,315.5
0036345,K.J. Osborn,WR,8.836363636363638,315.5
0026158,Joe Flacco,QB,19.55090909090909,317.5
0037304,Connor Heyward,TE,3.85,320.0
0039824,Jared Wiley,TE,3.9165780196198656,320.5
0036556,Brevin Jordan,TE,4.615,326.0
0031345,Jimmy Garoppolo,QB,15.493333333333332,328.5
0036754,Brock Wright,TE,4.345454545454546,331.5
0038567,Chad Ryland,K,4.4375,332.0
0037013,Zach Wilson,QB,12.040952380952382,332.5
0039847,Theo Johnson,TE,4.415314085056575,333.0
0033307,Kendrick Bourne,WR,7.970833333333334,334.0
0034279,Equanimeous St. Brown,WR,3.519047619047619,334.5
0034450,Greg Joseph,K,6.914285714285715,334.5
0033943,Josh Reynolds,WR,8.383870967741936,334.5
0038476,Tommy DeVito,QB,12.171111111111111,343.5
0038941,Tyler Scott,WR,2.761538461538462,349.0
0037012,Trey Lance,QB,6.23,357.5
0023853,Matt Prater,K,7.586206896551724,217.0
0033662,Cooper Rush,QB,4.66,359.0
0039376,Spencer Rattler,QB,11.516137267759564,361.0
0035249,Josh Oliver,TE,3.952,363.0
0035045,D.J. Montgomery,WR,7.300000000000001,364.0
0035973,Patrick Taylor,RB,2.4266666666666667,364.5
0039041,Elijah Higgins,TE,4.033333333333333,365.0
0039236,Johnny Wilson,WR,3.412530120481927,367.0
0039074,Davis Allen,TE,6.0600000000000005,367.0
0033217,Mo Alie-Cox,TE,3.814814814814815,368.5
0035358,Chase McLaughlin,K,8.5,237.0
0039684,Michael Wiley,RB,3.32198275862069,238.0
0033589,David Moore,WR,4.6375,371.0
0025565,Nick Folk,K,7.823529411764706,242.0
0037614,John Metchie III,WR,2.7,371.5
0030431,Robert Woods,WR,6.584375,373.0
0039238,Michael Pratt,QB,0.7140000000000003,522.0
0038152,Lucas Havrisik,K,6.888888888888889,399.0
0038416,Tyson Bagent,QB,11.852,380.0
0036422,Adam Trautman,TE,3.753571428571429,381.0
0038720,Julian Hill,TE,1.4666666666666668,382.0
0038598,Jaren Hall,QB,0.706666666666667,510.5
0039796,Rasheen Ali,RB,3.631216931216931,382.5
0034115,Jeff Wilson,RB,8.074074074074074,384.5
0039437,Cornelius Johnson,WR,0.8200000000000001,269.0
0039801,Devin Leary,QB,2.5809221311475414,583.5
0038647,Ronnie Bell,WR,3.85,386.0
0034613,Tanner Hudson,TE,5.7444444444444445,387.5
0039230,Dante Miller,RB,3.32198275862069,536.0
0039397,Tejhaun Palmer,WR,3.181701807228916,276.0
0037091,Bo Melton,WR,9.916666666666666,389.0
0032392,Austin Hooper,TE,4.418181818181819,389.5
0034646,Brandon Powell,WR,4.024137931034483,389.5
0038573,Derius Davis,WR,2.2642857142857147,520.5
0037429,Raheem Blackshear,RB,3.45,410.5
0030578,Cordarrelle Patterson,RB,7.664,281.0
0033895,Gerald Everett,TE,8.919354838709678,284.0
0039747,Ainias Smith,WR,4.143895582329318,394.0
0037506,Cole Fotheringham,TE,1.6,570.5
0031585,MyCole Pruitt,TE,4.2631578947368425,427.0
0039793,A.J. Barner,TE,4.039100321888412,395.0
0038041,Jake Ferguson,TE,8.906666666666666,291.0
0037569,Peyton Hendershot,TE,2.9562500000000003,396.0
0039451,Isaiah Williams,WR,2.9400000000000004,413.0
0037387,Jalen Virgil,WR,3.875,292.0
0037086,Grant Calcaterra,TE,3.0,414.0
0034173,Eddy Pineiro,K,7.625,298.0
0036415,Van Jefferson,WR,5.008333333333334,299.0
0035341,Trenton Irwin,WR,6.176190476190476,400.0
0037132,Britain Covey,WR,4.1,415.5
0031690,Eric Tomlinson,TE,3.6125,550.0
0033439,Pharaoh Brown,TE,3.84375,402.0
0037327,Skylar Thompson,QB,4.07,505.5
0032464,Kalif Raymond,WR,6.290909090909091,304.0
0032128,Chris Conley,WR,3.4285714285714284,403.5
0039462,Peter LeBlanc,WR,2.9400000000000004,310.0
0036425,DeeJay Dallas,RB,2.713636363636364,409.5
0034764,Michael Gallup,WR,6.912121212121213,311.0
0038640,Jalen Brooks,WR,4.133333333333334,428.5
0033246,Johnny Mundt,TE,3.539130434782609,407.5
0027973,Andy Dalton,QB,15.1175,410.0
0034577,Kyle Allen,QB,2.33,410.0
0036972,Mac Jones,QB,13.2192,418.5
0038465,Malik Heath,WR,2.7916666666666665,412.0
0032434,Brandon Allen,QB,0.78,413.0
0039364,Sione Vaki,RB,5.770166561248155,442.5
0037837,Calvin Austin III,WR,4.026666666666666,326.0
0037664,Alec Pierce,WR,6.384848484848486,414.5
0037168,Armani Rogers,TE,2.8000000000000003,522.5
0037175,Anthony Brown,QB,2.7899999999999996,432.0
0039521,Xavier Weaver,WR,2.9400000000000004,474.5
0031549,Nelson Agholor,WR,5.91875,332.0
0026300,Josh Johnson,QB,0.9300000000000002,446.5
0033119,Jacoby Brissett,QB,13.9675,333.0
0038394,Will Mallory,TE,3.5181818181818185,417.5
0036233,Donovan Peoples-Jones,WR,6.641379310344828,336.0
0039686,Colson Yankoff,RB,3.32198275862069,418.5
0038576,Charlie Jones,WR,2.9400000000000004,439.5
0033555,Mack Hollins,WR,7.702307692307693,419.0
0034364,Jordan Akins,TE,5.167857142857143,420.0
0031503,Jameis Winston,QB,7.764444444444444,421.0
0038128,Malik Willis,QB,2.488888888888889,521.0
0036816,Riley Patterson,K,7.0588235294117645,422.0
0037742,Treylon Burks,WR,6.55,347.0
0037299,Keaontay Ingram,RB,1.8882352941176472,349.0
0038391,Sean Clifford,QB,0.64,550.5
0026143,Matt Ryan,QB,15.773333333333332,350.0
0035537,Ty Johnson,RB,4.431578947368421,354.0
0035567,Craig Reynolds,RB,5.269230769230769,356.0
0037292,Andrew Ogletree,TE,3.5700000000000003,441.0
0037291,Jalen Nailor,WR,5.542857142857143,443.0
0035631,Drew Sample,TE,3.25625,486.0
0037077,Sam Howell,QB,18.322222222222223,361.0
0035594,Travis Homer,RB,4.788888888888889,485.5
0037745,Velus Jones Jr.,WR,2.5842105263157897,442.5
0039410,Ryan Flournoy,WR,2.095829471733086,462.5
0037112,Jake Tonges,TE,0.0,532.0
0036849,Tutu Atwell,WR,7.014814814814815,370.0
0036534,Shane Zylstra,TE,5.125,372.0
0037216,Justyn Ross,WR,2.26,375.0
0036860,Jake Funk,RB,3.2,445.5
0038797,Emanuel Wilson,RB,2.9285714285714284,380.0
0032104,Ameer Abdullah,RB,3.2793103448275867,381.0
0034860,Dante Pettis,WR,4.6571428571428575,594.5
0036898,Davis Mills,QB,11.484761904761903,528.5
0039449,James Turner,K,7.181818181818182,385.0
0037654,Tucker Fisk,TE,1.9,565.0
0035264,Jarrett Stidham,QB,16.656,386.0
0036423,Albert Okwuegbunam,TE,3.642857142857143,514.5
0039471,Frank Gore,RB,3.32198275862069,387.0
0039797,Jordan Travis,QB,9.354229508196722,556.0
0039649,Brayden Narveson,K,7.181818181818182,388.0
0035189,Matthew Wright,K,9.428571428571429,389.0
0037004,Hunter Long,TE,0.0,534.5
0035934,Trishton Jackson,WR,2.45,448.5
0034928,Steven Sims,WR,2.642857142857143,391.0
0038108,Bailey Zappe,QB,9.487142857142857,462.0
0039379,Jamari Thrash,WR,4.1,476.5
0037286,Kevin Harris,RB,3.611111111111111,397.0
0035298,Scott Miller,WR,4.07,509.5
0038389,Israel Abanikanda,RB,3.0500000000000003,401.0
0039052,Trey Palmer,WR,6.021052631578947,402.0
0038608,Kayshon Boutte,WR,1.3,580.5
0039573,Blake Watson,RB,3.32198275862069,562.5
0034054,River Cracraft,WR,4.24,494.5
0039229,Charlie Smyth,K,7.181818181818182,406.0
0038618,Xavier Hutchinson,WR,2.0615384615384618,580.5
0037451,Rod Williams,TE,0.0,577.0
0033869,Mitchell Trubisky,QB,11.546666666666669,455.5
0039603,British Brooks,RB,3.32198275862069,573.0
0033854,Dare Ogunbowale,RB,3.263157894736842,412.0
0034407,Ray-Ray McCloud,WR,3.965217391304348,413.0
0035639,Parris Campbell,WR,7.18,415.0
0035831,James Robinson,RB,7.716666666666666,532.0
0035100,Jake Browning,QB,19.38222222222222,416.0
0036630,Tylan Wallace,WR,1.342857142857143,464.5
0036876,Kylen Granson,TE,4.792857142857143,419.0
0037257,David Bell,WR,4.479166666666667,422.0
0039703,Cody Schrader,RB,3.32198275862069,462.0
0038809,Ben Sims,TE,1.9285714285714288,651.5
0038606,Parker Washington,WR,6.533333333333334,425.0
0038705,Emari Demercado,RB,6.663636363636363,430.0
0038738,Hunter Luepke,RB,2.1166666666666667,602.0
0034272,Marquez Valdes-Scantling,WR,6.0725,466.0
0033251,Zach Pascal,WR,1.9333333333333331,506.5
0038935,Brenton Strange,TE,2.8999999999999995,491.5
0039392,Jha'Quan Jackson,WR,3.536310269650029,667.5
0033591,Noah Brown,WR,8.214285714285714,438.0
0033466,Isaiah McKenzie,WR,5.736000000000001,470.5
0035572,Parker Hesse,TE,1.9888888888888887,534.0
0035704,Drew Lock,QB,8.780000000000001,444.0
0033576,Eric Saubert,TE,2.857142857142857,510.5
0035329,Donald Parham,TE,5.74,445.0
0037805,Jeremy Ruckert,TE,2.7416666666666667,473.0
0035406,Lil'Jordan Humphrey,WR,3.6571428571428575,447.0
0036412,KJ Hamler,WR,4.3,447.0
0035215,Deonte Harty,WR,2.8125,537.5
0039386,Bub Means,WR,3.948840579710145,449.0
0036271,Quez Watkins,WR,5.2375,450.0
0033292,Trent Taylor,WR,1.5,515.0
0036842,Kene Nwangwu,RB,1.1333333333333335,453.0
0033958,Nathan Peterman,QB,2.465,515.5
0036924,Michael Carter,RB,5.961290322580646,454.0
0037539,Lucas Krull,TE,3.357142857142857,496.5
0037563,Malik Davis,RB,5.733333333333333,459.0
0037557,Ronnie Rivers,RB,3.75,461.0
0038979,Cedric Tillman,WR,4.0181818181818185,462.0
0039136,Zach Evans,RB,0.475,463.0
0036429,Charlie Woerner,TE,1.24,560.5
0038611,Chris Rodriguez Jr.,RB,4.737500000000001,464.0
0035645,Gunner Olszewski,WR,1.1,548.0
0038496,Xavier Gipson,WR,4.05,467.0
0038627,Antoine Green,WR,0.6,609.0
0038685,Chris Brooks,RB,3.5333333333333337,468.0
0039420,Jaheim Bell,TE,1.6844849785407727,484.5
0034365,Ian Thomas,TE,2.5650000000000004,529.5
0038951,Sean Tucker,RB,1.7333333333333334,469.0
0039424,Devaughn Vele,WR,1.5,631.0
0039393,Jase McClellan,RB,2.7416245791245797,471.0
0036918,Jacob Harris,WR,1.6,575.0
0039589,Kameron Johnson,WR,2.9400000000000004,473.0
0028118,Tyrod Taylor,QB,9.388333333333334,475.0
0035145,Austin Seibert,K,6.25,545.0
0035021,Ashton Dulin,WR,6.071428571428571,477.0
0039754,Devin Culp,TE,1.228,478.0
0031547,DeVante Parker,WR,7.132000000000001,479.0
0032268,Marcus Mariota,QB,16.021333333333335,480.0
0032134,C.J. Uzomah,TE,4.0,481.0
0036628,John Bates,TE,2.821739130434783,500.5
0036887,Luke Farrell,TE,2.8076923076923075,580.5
0038046,Charlie Kolar,TE,4.0,591.0
0036268,Laviska Shenault Jr.,WR,4.485714285714286,569.0
0034838,Royce Freeman,RB,5.015384615384615,491.0
0032398,Chris Moore,WR,6.213333333333334,531.5
0034771,Mason Rudolph,QB,13.264,541.5
0034159,Will Dissly,TE,4.707407407407408,496.0
0034414,Boston Scott,RB,3.230769230769231,497.0
0034487,Trent Sherfield,WR,4.111111111111111,498.0
0034981,Foster Moreau,TE,5.332000000000001,499.0
0038122,Desmond Ridder,QB,13.361052631578948,596.0
0035291,Trayveon Williams,RB,1.2722222222222224,500.0
0034970,Irv Smith,TE,5.333333333333333,502.0
0035208,Olamide Zaccheaus,WR,5.353571428571429,503.0
0036331,Devin Duvernay,WR,5.87,505.0
0036232,Harrison Bryant,TE,3.9321428571428574,506.0
0036020,Salvon Ahmed,RB,4.635714285714286,508.0
0036182,Nick Westbrook-Ikhine,WR,6.096428571428572,510.0
0036497,Tim Jones,WR,1.946153846153846,511.0
0036510,Kenny Yeboah,TE,2.4000000000000004,512.0
0037005,Tommy Tremble,TE,4.2518518518518515,514.0
0037011,Tre' McKitty,TE,2.3111111111111113,517.0
0039365,Jacob Cowing,WR,4.734431231419183,520.0
0036741,Jack Stoll,TE,2.24,521.0
0037306,John FitzPatrick,TE,2.2,522.0
0037282,James Mitchell,TE,3.31,524.0
0037120,Tyler Goodson,RB,3.016666666666667,525.0
0038098,Pierre Strong,RB,4.117647058823529,529.0
0037093,Dareke Young,WR,3.25,533.0
0039498,Harrison Mevis,K,7.181818181818182,536.0
0037382,Brandon Johnson,WR,6.4,537.0
0038583,Dorian Thompson-Robinson,QB,3.4428571428571426,544.0
0038589,Josh Whyle,TE,3.05,546.0
0039050,Payne Durham,TE,1.9428571428571428,550.0
0039576,Spencer Shrader,K,7.181818181818182,552.0
0031428,Allen Robinson,WR,5.773076923076923,
0039677,Sam Hartman,QB,6.647000000000001,565.0
0031941,Jamison Crowder,WR,4.536363636363637,567.0
0032208,Phillip Dorsett,WR,3.23125,568.0
0031484,Chris Manhertz,TE,1.38,569.0
0031800,Taylor Heinicke,QB,15.425714285714289,571.0
0032436,Jeff Driskel,QB,4.6450000000000005,573.0
0033936,C.J. Beathard,QB,2.246,575.0
0033319,Nick Mullens,QB,11.61111111111111,583.0
0033757,Robert Tonyan,TE,4.792857142857143,584.0
0034367,Nyheim Hines,RB,4.027777777777778,588.0
0034575,David Wells,TE,1.0,593.0
0034401,Mike White,QB,7.064000000000002,594.0
0034438,Logan Woodside,QB,-0.3199999999999998,595.0
0034208,Mike Boone,RB,2.806666666666666,597.0
0034854,KhaDarel Hodge,WR,3.563636363636364,598.0
0034899,John Wolford,QB,8.4,599.0
0035311,Myles Gaskin,RB,4.7,600.0
0039271,Jarveon Howard,RB,3.32198275862069,601.0
0039256,Riley Sharp,TE,2.6975,602.0
0039244,Chris Collier,RB,3.32198275862069,604.0
0039308,TaMerik Williams,RB,3.32198275862069,606.0
0035222,Zach Gentry,TE,2.3000000000000003,608.0
0035624,N'Keal Harry,WR,4.1000000000000005,609.0
0039575,Jason Bean,QB,6.647000000000001,611.0
0034955,Brett Rypien,QB,4.8,612.0
0039464,Austin Reed,QB,6.647000000000001,615.0
0035282,Easton Stick,QB,14.712,616.0
0039306,Dee Williams,WR,2.9400000000000004,617.0
0039529,Nathaniel Peat,RB,3.32198275862069,620.0
0039629,Elijah Collins,RB,3.32198275862069,624.0
0039657,Casey Bauman,QB,6.647000000000001,626.0
0039456,John Rhys Plumlee,QB,6.647000000000001,629.0
0035628,D'Ernest Johnson,RB,2.2333333333333334,630.0
0035993,Tyler Huntley,QB,9.825454545454546,633.0
0036165,Isaiah Hodgins,WR,7.56,635.0
0039874,Jude McAtamney,K,7.181818181818182,638.0
0035771,Mitchell Wilcox,TE,2.77,639.0
0036032,Sean McKeon,TE,0.775,641.0
0036439,Tyrie Cleveland,WR,1.6000000000000003,642.0
0036332,Josiah Deguara,TE,2.992307692307693,644.0
0036297,Darrynton Evans,RB,4.522222222222222,645.0
0039737,Tip Reiman,TE,5.469034334763945,646.0
0036254,Collin Johnson,WR,2.1,648.0
0036255,Denzel Mims,WR,3.288888888888889,652.0
0036269,La'Mical Perine,RB,7.000000000000001,653.0
0035924,Jonathan Ward,RB,2.1,654.0
0035806,JaMycal Hasty,RB,4.70625,657.0
0036059,Tyler Mabry,TE,7.7,659.0
0036247,Freddie Swain,WR,3.8,660.0
0036219,Marquez Callaway,WR,3.78,662.0
0036145,Lawrence Cager,TE,4.3999999999999995,665.0
0036527,DJ Turner,WR,1.3,668.0
0036950,Avery Williams,RB,2.5714285714285716,671.0
0036955,Terrace Marshall Jr.,WR,5.519047619047619,672.0
0036646,Simi Fehoko,WR,4.433333333333334,673.0
0036879,Sam Ehlinger,QB,10.405,675.0
0036862,Ben Skowronek,WR,4.470833333333334,679.0
0036635,Ihmir Smith-Marsette,WR,2.69,682.0
0036928,Kyle Trask,QB,0.2733333333333334,685.0
0036590,Quintin Morris,TE,3.4799999999999995,686.0
0036755,Jaret Patterson,RB,7.800000000000001,689.0
0036959,Michael Bandy,WR,2.6999999999999997,690.0
0039259,Dayton Wade,WR,2.9400000000000004,691.0
0039254,Mike Rigerman,TE,2.6975,692.0
0037085,Tyler Badie,RB,9.4,694.0
0037272,Montrell Washington,WR,0.8,698.0
0037098,Samori Toure,WR,3.0833333333333335,699.0
0037617,Hassan Haskins,RB,3.7142857142857144,700.0
0039311,Emani Bailey,RB,3.32198275862069,702.0
0037265,Snoop Conner,RB,2.04,703.0
0039302,Kobe Lewis,RB,3.32198275862069,704.0
0039730,Isaiah Wooden,WR,2.9400000000000004,705.0
0038092,Erik Ezukanma,WR,1.1666666666666667,707.0
0038104,Tyquan Thornton,WR,4.4523809523809526,709.0
0039316,Baylor Cupp,TE,2.6975,710.0
0039511,Ahmarean Brown,WR,2.9400000000000004,712.0
0038045,Isaiah Spiller,RB,1.9571428571428573,713.0
0037827,Tyrion Davis-Price,RB,2.0,714.0
0037828,Danny Gray,WR,0.58,715.0
0039501,Jack Plummer,QB,6.647000000000001,718.0
0039872,Thomas Yassmin,TE,2.6975,720.0
0039314,Phillip Brooks,WR,2.9400000000000004,723.0
0039299,George Holani,RB,3.32198275862069,724.0
0039648,David Martin-Robinson,TE,2.6975,727.0
0039695,Jacob Warren,TE,2.6975,728.0
0037594,Julius Chestnut,RB,2.766666666666667,729.0
0039701,Terique Owens,WR,2.9400000000000004,731.0
0039711,Trey Knox,TE,2.6975,733.0
0039678,Austin Jones,RB,3.32198275862069,734.0
0039534,Carter Bradley,QB,6.647000000000001,735.0
0037348,Tanner Conner,WR,0.0,738.0
0037355,Braylon Sanders,WR,0.8500000000000001,739.0
0037410,Jashaun Corbin,RB,4.3,740.0
0039577,Kedon Slovis,QB,6.647000000000001,741.0
0037487,Deven Thompkins,WR,3.00625,744.0
0038147,Daylen Baldwin,WR,4.5,747.0
0037708,Maurice Alexander,WR,1.7000000000000002,748.0
0038149,Josh Ali,WR,0.0,749.0
0038582,Clayton Tune,QB,1.2466666666666666,762.0
0039798,Isaiah Davis,RB,3.272131478120589,767.0
0038492,Jason Brownlee,WR,4.15,768.0
0038678,Jordan Mims,RB,0.0,772.0
0038891,Shedrick Jackson,WR,0.0,773.0
0038783,Nate Adkins,TE,1.55,776.0
0038667,Elijah Cooks,WR,3.4000000000000004,779.0
0038862,Elijah Dotson,RB,3.9,784.0
0038821,Rakim Jarrett,WR,1.6666666666666667,785.0
0039394,Casey Washington,WR,3.376706827309239,790.0
0039814,Erick All,TE,4.27673860812781,791.0
0039399,Tanner McLachlan,TE,2.715545779685265,792.0
0039405,Jawhar Jordan,RB,1.971428571428572,795.0
0039385,Keilan Robinson,RB,3.589506172839506,796.0
0032394,Nick Vannett,TE,2.1333333333333333,802.0
0032792,Nate Sudfeld,QB,-0.2,803.0
0038558,Darnell Washington,TE,1.4555555555555555,804.0
0034073,Ross Dwelley,TE,3.616666666666667,815.0
0034104,Cam Sims,WR,1.4083333333333334,816.0
0034177,Tim Boyle,QB,2.505,819.0
0035544,Tom Kennedy,WR,4.42,825.0
0036133,James Proche,WR,1.3555555555555556,827.0
0035876,Andre Baccellia,WR,2.3,829.0
0035891,Cody White,WR,1.2,830.0
0035860,Tony Jones,RB,4.733333333333333,833.0
0036387,Jalen Reagor,WR,2.6,835.0
0036176,Mason Kinsey,WR,1.4500000000000002,838.0
0036652,Racey McMath,WR,1.6,847.0
0036858,Jalen Camp,WR,1.7000000000000002,848.0
0036569,Gary Brightwell,RB,3.466666666666667,851.0
0036877,Jaelon Darden,WR,1.6000000000000003,853.0
0037475,Stone Smartt,TE,2.546666666666667,855.0
0038632,Colton Dowell,WR,0.65,857.0
0038397,Evan Hull,RB,1.7000000000000002,858.0
0039816,Anthony Gould,WR,4.47812248995984,899.0
0039231,Patrick Murtagh,TE,2.6975,900.0
</file>

<file path="build.sh">
#!/bin/bash

# Define your Docker image name
IMAGE_NAME="fantasy-football-ai"
TAG="latest"

echo "Building Docker image: ${IMAGE_NAME}:${TAG}"

# Build the Docker image
docker build -t "${IMAGE_NAME}:${TAG}" .

if [ $? -eq 0 ]; then
    echo "Docker image built successfully: ${IMAGE_NAME}:${TAG}"
else
    echo "Failed to build Docker image."
    exit 1
fi
</file>

<file path="config.py">
import os

class Config:
    """
    Configuration settings for the Fantasy Football Draft AI project.
    """
    # --- Project Paths ---
    BASE_DIR = os.path.dirname(os.path.abspath(__file__))
    DATA_DIR = os.path.join(BASE_DIR, 'data')
    MODELS_DIR = os.path.join(BASE_DIR, 'models')
    LOGS_DIR = os.path.join(BASE_DIR, 'logs')
    PLAYER_DATA_CSV = os.path.join(DATA_DIR, 'merged_player_data_cleaned.csv')

    # Ensure directories exist
    os.makedirs(DATA_DIR, exist_ok=True)
    os.makedirs(MODELS_DIR, exist_ok=True)
    os.makedirs(LOGS_DIR, exist_ok=True)

    # --- Draft Environment Parameters ---
    NUM_TEAMS = 10
    AGENT_START_POSITION = 10 # Our agent's pick order (1-indexed)
    ROSTER_STRUCTURE = {
        'QB': 1,
        'RB': 2,
        'WR': 3,
        'TE': 1,
        'FLEX': 2, # FLEX can be RB, WR, or TE
    }
    BENCH_MAXES = {
        'QB': 1, # Max QBs on bench
        'RB': 3, # Max RBs on bench
        'WR': 3, # Max WRs on bench
        'TE': 1, # Max TEs on bench
    }
    COMPETING_TEAM_LOGIC = 'HEURISTIC' # Options: 'ADP', 'HEURISTIC'

    # --- Data Preprocessing Parameters ---
    # Configuration for mock ADP generation if 'adp' column is missing
    MOCK_ADP_CONFIG = {
        'enabled': True, # Set to False if you want an error when adp is missing
        'weights': {
            'projected_points': 1.0, # Higher weight means higher points => lower ADP
            # Add other attributes here if available in CSV and you want to use them
            # 'percentage_on_field': 0.5,
        },
        'sort_order_ascending': False # True for lower values = lower ADP (e.g., age), False for higher values = lower ADP (e.g., points)
    }

    # --- Reinforcement Learning Parameters ---
    TOTAL_EPISODES = 30000
    LEARNING_RATE = 0.0005
    DISCOUNT_FACTOR = 0.99 # Gamma

    # --- State Space Parameters ---
    # A list of all potential state features. The env will use enabled_state_features.
    ALL_STATE_FEATURES = [
        "best_available_qb_points",
        "best_available_rb_points",
        "best_available_wr_points",
        "best_available_te_points",
        "current_roster_qb_count",
        "current_roster_rb_count",
        "current_roster_wr_count",
        "current_roster_te_count",
        "available_roster_slots_qb",
        "available_roster_slots_rb",
        "available_roster_slots_wr",
        "available_roster_slots_te",
        "available_roster_slots_flex",
        "qb_available_flag",
        "rb_available_flag",
        "wr_available_flag",
        "te_available_flag",
        "current_pick_number", # New: added
        "agent_start_position", # New: added
    ]
    # Select which features to enable for the agent's state vector
    ENABLED_STATE_FEATURES = [
        "best_available_qb_points",
        "best_available_rb_points",
        "best_available_wr_points",
        "best_available_te_points",
        "current_roster_qb_count",
        "current_roster_rb_count",
        "current_roster_wr_count",
        "current_roster_te_count",
        "available_roster_slots_qb",
        "available_roster_slots_rb",
        "available_roster_slots_wr",
        "available_roster_slots_te",
        "available_roster_slots_flex",
        "qb_available_flag",
        "rb_available_flag",
        "wr_available_flag",
        "te_available_flag",
        "current_pick_number",
        "agent_start_position",
    ]
    # State normalization method
    STATE_NORMALIZATION_METHOD = 'min_max' # Options: 'min_max', 'z_score', 'none'

    # --- Reward Function Parameters ---
    INVALID_ACTION_PENALTIES = {
    'already_drafted': -100, # Much smaller penalty
    'roster_full_QB': -50,
    'roster_full_RB': -40,
    'roster_full_WR': -40,
    'roster_full_TE': -50,
    'no_players_available': -5, # Added this penalty key previously for completeness
    'default_invalid': -50
    }
    # New: Flag to enable/disable invalid action penalties
    ENABLE_INVALID_ACTION_PENALTIES = True # Set to False for "simple reward mode"


    ENABLE_INTERMEDIATE_REWARD = True # True for small reward for valid picks
    INTERMEDIATE_REWARD_VALUE = 30 # Reward for each successful valid pick if enabled

    # New: Bonus for successfully drafting an entire team
    BONUS_FOR_FULL_ROSTER = 200 # A significant positive bonus (e.g., 100 or 200)

    # --- Neural Network Architecture ---
    HIDDEN_DIM = 64

    # --- Simulation and Evaluation Parameters ---
    # Path to a trained model (.pth file) to load for simulation/evaluation
    MODEL_PATH_TO_LOAD = os.path.join(MODELS_DIR, "reinforce_policy_model_20250608_194848.pth") # REPLACE with your actual model path
    NUM_SIMULATION_RUNS = 5 # How many times to run the draft simulation

    # You might want to temporarily override some env settings for simulation if different from training
    # SIM_COMPETING_TEAM_LOGIC = 'ADP' # For example, always test against ADP opponents
    # SIM_AGENT_START_POSITION = 7 # Test a specific draft position

# You can access configuration like: `Config.TOTAL_EPISODES`
</file>

<file path="data_utils.py">
import os
import pandas as pd
import numpy as np
from dataclasses import dataclass, field
from typing import List, Dict, Optional

# Define Player data structure
@dataclass
class Player:
    player_id: int
    name: str
    position: str # QB, RB, WR, TE
    projected_points: float
    adp: float = field(default=np.inf) # Default to infinity if no ADP

def _generate_mock_adp(players: List[Player], adp_config: Dict) -> List[Player]:
    """
    Generates mock ADP for players based on configurable weighted attributes.
    Modifies players in place.
    """
    if not adp_config['enabled']:
        raise ValueError("Mock ADP generation is disabled but 'adp' column is missing.")

    # Create a list of tuples (weighted_score, player_id)
    weighted_scores = []
    for player in players:
        score = 0.0
        for attr, weight in adp_config['weights'].items():
            if hasattr(player, attr):
                score += getattr(player, attr) * weight
            else:
                print(f"Warning: Attribute '{attr}' not found in Player object for mock ADP generation.")
        weighted_scores.append((score, player.player_id))

    # Sort based on weighted scores
    # If sort_order_ascending is True, lower score gets lower ADP (e.g., age)
    # If sort_order_ascending is False, higher score gets lower ADP (e.g., projected points)
    weighted_scores.sort(key=lambda x: x[0], reverse=not adp_config['sort_order_ascending'])

    # Assign ADPs based on rank
    player_id_to_adp = {score_tuple[1]: i + 1 for i, score_tuple in enumerate(weighted_scores)}

    # Update player objects with new ADP
    for player in players:
        player.adp = player_id_to_adp.get(player.player_id, np.inf) # Assign generated ADP or keep inf

    print(f"Generated mock ADP for {len(players)} players based on weights: {adp_config['weights']}")
    return players


def load_player_data(filepath: str, adp_config: Dict) -> List[Player]:
    """
    Loads player data from a CSV file, handles missing ADP by generating mock ADP.
    """
    try:
        df = pd.read_csv(filepath)
    except FileNotFoundError:
        print(f"Error: Player data CSV not found at {filepath}. Please ensure it exists.")
        print("Creating a dummy CSV for demonstration purposes...")
        _create_dummy_csv(filepath)
        df = pd.read_csv(filepath) # Try loading again

    required_columns = ['player_id', 'name', 'position', 'projected_points']
    for col in required_columns:
        if col not in df.columns:
            raise ValueError(f"Missing required column: '{col}' in player data CSV.")

    players = []
    has_adp_column = 'adp' in df.columns

    for _, row in df.iterrows():
        player_id = int(row['player_id'])
        name = str(row['name'])
        position = str(row['position']).upper() # Ensure consistent casing
        projected_points = float(row['projected_points'])
        adp = float(row['adp']) if has_adp_column and pd.notna(row['adp']) else np.inf

        players.append(Player(player_id, name, position, projected_points, adp))

    # If ADP column was missing or all values were NaN, generate mock ADP
    if not has_adp_column or all(p.adp == np.inf for p in players):
        print("ADP column missing or all values NaN. Generating mock ADP...")
        players = _generate_mock_adp(players, adp_config)
    else:
        print(f"Loaded {len(players)} players with existing ADP.")

    # Sort players by ADP for easier processing later (lower ADP means drafted earlier)
    players.sort(key=lambda p: p.adp)

    return players

def _create_dummy_csv(filepath: str):
    """
    Creates a small dummy CSV file for initial testing if the actual file is not found.
    """
    dummy_data = """player_id,name,position,projected_points,adp
1001,Patrick Mahomes,QB,378.5,15.2
1002,Josh Allen,QB,350.1,25.5
1003,Christian McCaffrey,RB,320.0,1.1
1004,Austin Ekeler,RB,290.5,5.8
1005,Justin Jefferson,WR,310.0,3.2
1006,Ja'Marr Chase,WR,295.5,7.5
1007,Travis Kelce,TE,280.0,10.0
1008,Mark Andrews,TE,250.0,20.1
1009,Saquon Barkley,RB,280.0,9.0
1010,Cooper Kupp,WR,270.0,12.0
1011,Jalen Hurts,QB,340.0,30.0
1012,Jonathan Taylor,RB,270.0,11.0
1013,Tyreek Hill,WR,280.0,14.0
1014,T.J. Hockenson,TE,200.0,35.0
1015,Stefon Diggs,WR,260.0,16.0
1016,Nick Chubb,RB,250.0,13.0
1017,CeeDee Lamb,WR,255.0,18.0
1018,Amari Cooper,WR,240.0,22.0
1019,George Kittle,TE,220.0,28.0
1020,Dak Prescott,QB,300.0,40.0
"""
    os.makedirs(os.path.dirname(filepath), exist_ok=True)
    with open(filepath, 'w') as f:
        f.write(dummy_data)
    print(f"Dummy CSV created at: {filepath}")
</file>

<file path="Dockerfile">
# Use an official Python runtime as a parent image
# We choose a specific version (e.g., 3.10-slim-buster) for stability
FROM python:3.10-slim-buster

# Set the working directory in the container
WORKDIR /app

# Install system dependencies if needed (e.g., for some ML libraries)
# For now, minimal additions. If you encounter errors later related to
# dependencies like 'blas', 'lapack', etc., you might need to add:
# RUN apt-get update && apt-get install -y --no-install-recommends \
#     build-essential \
#     libblas-dev \
#     liblapack-dev \
#     gfortran \
#     && rm -rf /var/lib/apt/lists/*

# Copy the requirements file into the container
COPY requirements.txt .

# Install any specified Python packages
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of your application code into the container
# This copies the entire 'your_project' content into '/app'
COPY . .

# Create the data, models, and logs directories if they don't exist
# These directories are created by config.py, but explicit creation here
# ensures they are present even if config.py isn't run directly during image build
RUN mkdir -p data models logs

# Define environment variables (optional, but good practice)
ENV PYTHONUNBUFFERED 1

# Command to run your application
# This is a placeholder. The 'run.sh' script will typically override this
# with a specific command like 'python train.py'
CMD ["python", "--version"]
</file>

<file path="fantasy_draft_env.py">
import gym
from gym import spaces
import numpy as np
from collections import defaultdict
import math
import random
from typing import Optional, Dict, List, Tuple # Added List, Tuple for _try_select_player_for_team return type

from config import Config
from data_utils import load_player_data, Player

class FantasyFootballDraftEnv(gym.Env):
    """
    Custom OpenAI Gym Environment for a Fantasy Football Draft.
    The agent learns to pick optimal positions to maximize projected team points.
    """
    metadata = {'render_modes': ['human'], 'render_fps': 4}

    def __init__(self, config: Config):
        super(FantasyFootballDraftEnv, self).__init__()
        self.config = config

        # --- Load Players Data ---
        self.all_players_data = load_player_data(config.PLAYER_DATA_CSV, config.MOCK_ADP_CONFIG)
        self.player_map = {p.player_id: p for p in self.all_players_data}

        # --- Environment Dimensions ---
        # Action Space: 4 discrete actions (select QB, RB, WR, or TE)
        # Note: FLEX is not an action, but a roster slot that RB/WR/TE can fill.
        self.action_to_position = {0: 'QB', 1: 'RB', 2: 'WR', 3: 'TE'}
        self.position_to_action = {'QB': 0, 'RB': 1, 'WR': 2, 'TE': 3}
        self.action_space = spaces.Discrete(len(self.action_to_position))

        # State Space: Dynamically determined by ENABLED_STATE_FEATURES
        self.state_features_map = {
            # Player availability info
            "best_available_qb_points": self._get_best_available_qb_points,
            "best_available_rb_points": self._get_best_available_rb_points,
            "best_available_wr_points": self._get_best_available_wr_points,
            "best_available_te_points": self._get_best_available_te_points,
            "qb_available_flag": lambda: 1 if any(p.position == 'QB' and p.player_id in self.available_players_ids for p in self.all_players_data) else 0,
            "rb_available_flag": lambda: 1 if any(p.position == 'RB' and p.player_id in self.available_players_ids for p in self.all_players_data) else 0,
            "wr_available_flag": lambda: 1 if any(p.position == 'WR' and p.player_id in self.available_players_ids for p in self.all_players_data) else 0,
            "te_available_flag": lambda: 1 if any(p.position == 'TE' and p.player_id in self.available_players_ids for p in self.all_players_data) else 0,
            # Current Roster Counts (for agent's team)
            "current_roster_qb_count": lambda: self.teams_rosters[self.agent_team_id]['QB'],
            "current_roster_rb_count": lambda: self.teams_rosters[self.agent_team_id]['RB'],
            "current_roster_wr_count": lambda: self.teams_rosters[self.agent_team_id]['WR'],
            "current_roster_te_count": lambda: self.teams_rosters[self.agent_team_id]['TE'],
            # Available Roster Slots (for agent's team)
            "available_roster_slots_qb": lambda: self.config.ROSTER_STRUCTURE['QB'] + self.config.BENCH_MAXES['QB'] - self.teams_rosters[self.agent_team_id]['QB'],
            "available_roster_slots_rb": lambda: self.config.ROSTER_STRUCTURE['RB'] + self.config.BENCH_MAXES['RB'] - self.teams_rosters[self.agent_team_id]['RB'],
            "available_roster_slots_wr": lambda: self.config.ROSTER_STRUCTURE['WR'] + self.config.BENCH_MAXES['WR'] - self.teams_rosters[self.agent_team_id]['WR'],
            "available_roster_slots_te": lambda: self.config.ROSTER_STRUCTURE['TE'] + self.config.BENCH_MAXES['TE'] - self.teams_rosters[self.agent_team_id]['TE'],
            "available_roster_slots_flex": lambda: self.config.ROSTER_STRUCTURE['FLEX'] - self.teams_rosters[self.agent_team_id]['FLEX'], # Simplified flex calculation
            # Draft Progression Context
            "current_pick_number": lambda: self.current_pick_number,
            "agent_start_position": lambda: self.config.AGENT_START_POSITION,
        }
        self.observation_space_dim = len(config.ENABLED_STATE_FEATURES)
        self.observation_space = spaces.Box(low=-np.inf, high=np.inf, shape=(self.observation_space_dim,), dtype=np.float32)

        # --- Internal Game State (will be reset for each episode) ---
        self.available_players_ids = set() # Set of player_ids currently available
        self.teams_rosters = defaultdict(lambda: {'QB': 0, 'RB': 0, 'WR': 0, 'TE': 0, 'FLEX': 0, 'PLAYERS': []}) # Tracks counts and actual players for each team
        self.draft_order = [] # List of team_ids in draft order for current episode
        self.current_pick_idx = 0 # Index in self.draft_order
        self.current_pick_number = 0 # Global pick number (1-indexed)
        self.agent_team_id = self.config.AGENT_START_POSITION # Agent's unique ID (matches its start position)

        # Pre-calculate total roster size for done check
        self.total_roster_size_per_team = sum(self.config.ROSTER_STRUCTURE.values()) + sum(self.config.BENCH_MAXES.values())
        # The sum of required spots plus bench spots for each position.
        # Flex players reduce available flex slots but increase RB/WR/TE counts,
        # so this is complex. For now, ensure we don't overfill total_roster_size.


    def _get_best_available_qb_points(self):
        best_qb = self._get_best_available_player_by_pos('QB')
        return best_qb.projected_points if best_qb else 0

    def _get_best_available_rb_points(self):
        best_rb = self._get_best_available_player_by_pos('RB')
        return best_rb.projected_points if best_rb else 0

    def _get_best_available_wr_points(self):
        best_wr = self._get_best_available_player_by_pos('WR')
        return best_wr.projected_points if best_wr else 0

    def _get_best_available_te_points(self):
        best_te = self._get_best_available_player_by_pos('TE')
        return best_te.projected_points if best_te else 0

    def reset(self, seed: Optional[int] = None, options: Optional[Dict] = None):
        super().reset(seed=seed)

        # Reset all internal game state variables
        self.available_players_ids = {p.player_id for p in self.all_players_data}
        self.teams_rosters = defaultdict(lambda: {'QB': 0, 'RB': 0, 'WR': 0, 'TE': 0, 'FLEX': 0, 'PLAYERS': []})
        self.current_pick_idx = 0
        self.current_pick_number = 1 # Start at pick 1

        # Generate draft order (snake draft)
        self.draft_order = self._generate_snake_draft_order(self.config.NUM_TEAMS, self.total_roster_size_per_team)

        # IMPORTANT: Advance the draft until it's the agent's turn
        # Simulate picks by other teams until the agent's turn arrives or the draft ends.
        while self.current_pick_idx < len(self.draft_order) and \
              self.draft_order[self.current_pick_idx] != self.agent_team_id:

            current_sim_team_id = self.draft_order[self.current_pick_idx]
            sim_drafted_player = self._simulate_competing_pick(current_sim_team_id)

            if sim_drafted_player:
                self.teams_rosters[current_sim_team_id]['PLAYERS'].append(sim_drafted_player)
                self.available_players_ids.remove(sim_drafted_player.player_id)
                self._update_roster_counts(current_sim_team_id, sim_drafted_player)
            else:
                # If a competing team cannot make a pick (e.g., no players left that fit their needs/roster)
                # and no players are available globally, the draft may effectively end early.
                if not self.available_players_ids:
                    print(f"[{self.current_pick_number}] No players left in pool. Ending draft early during reset advance.")
                    break # End the reset advance loop if no more players to pick
                # If there are still players, but the sim team couldn't pick (e.g., specific needs not met),
                # we just advance the pick counter and hope for other teams.
                # This could indicate a need for more robust sim team logic or draft end conditions.
                print(f"[{self.current_pick_number}] Warning: Competing team {current_sim_team_id} could not make a valid pick.")


            self.current_pick_idx += 1
            self.current_pick_number += 1
        
        # After this loop, it's either the agent's turn, or the draft has already completed
        # or there are no more players available at all.

        observation = self._get_state()
        info = self._get_info() 
        
        # If the draft ended before the agent even got its first pick (e.g., very late draft position in a short draft)
        if self.current_pick_idx >= len(self.draft_order) and len(self.teams_rosters[self.agent_team_id]['PLAYERS']) == 0:
            info['episode_ended_before_agent_first_pick'] = True
            # The agent will get a 0 reward for this episode if it couldn't make any picks.

        return observation, info

    def step(self, action: int):
        current_team_id = self.draft_order[self.current_pick_idx]
        assert current_team_id == self.agent_team_id, f"Assertion failed: It's not the agent's turn! Expected {self.agent_team_id}, Got {current_team_id} at pick {self.current_pick_number}"

        selected_position = self.action_to_position[action]
        reward = 0
        done = False
        info = {}

        # --- 1. Agent's Turn ---
        is_valid_pick, drafted_player = self._try_select_player_for_team(
            self.agent_team_id, selected_position, self.available_players_ids
        )

        if not is_valid_pick:
            # If invalid action, episode terminates.
            # Apply penalty only if ENABLE_INVALID_ACTION_PENALTIES is True.
            if self.config.ENABLE_INVALID_ACTION_PENALTIES:
                penalty_key = f'roster_full_{selected_position}'
                if not self._get_best_available_player_by_pos(selected_position):
                    penalty_key = 'no_players_available'
                reward += self.config.INVALID_ACTION_PENALTIES.get(penalty_key, self.config.INVALID_ACTION_PENALTIES['default_invalid'])
            
            done = True # Invalid action still ends the episode for safety/clarity
            info['invalid_action'] = True
            info['reason'] = f"Invalid pick: {selected_position} - {'Penalty applied' if self.config.ENABLE_INVALID_ACTION_PENALTIES else 'No penalty'}"
        else:
            # Valid pick
            self.teams_rosters[self.agent_team_id]['PLAYERS'].append(drafted_player)
            self.available_players_ids.remove(drafted_player.player_id)
            info['drafted_player'] = drafted_player.name
            info['drafted_position'] = drafted_player.position
            info['drafted_points'] = drafted_player.projected_points
            info['drafted_adp'] = drafted_player.adp

            # Update position counts
            self._update_roster_counts(self.agent_team_id, drafted_player)

            if self.config.ENABLE_INTERMEDIATE_REWARD:
                # Change the following line:
                reward += self.config.INTERMEDIATE_REWARD_VALUE
                # TO THIS:
                # reward += drafted_player.projected_points * 3

        self.current_pick_idx += 1
        self.current_pick_number += 1

        # --- 2. Simulate Competing Teams' Turns ---
        if not done: # Only simulate if agent's pick was valid
            while self.current_pick_idx < len(self.draft_order) and \
                  self.draft_order[self.current_pick_idx] != self.agent_team_id:

                current_sim_team_id = self.draft_order[self.current_pick_idx]
                sim_drafted_player = self._simulate_competing_pick(current_sim_team_id)

                if sim_drafted_player:
                    self.teams_rosters[current_sim_team_id]['PLAYERS'].append(sim_drafted_player)
                    self.available_players_ids.remove(sim_drafted_player.player_id)
                    self._update_roster_counts(current_sim_team_id, sim_drafted_player)
                else:
                    print(f"[{self.current_pick_number}] Warning: Competing team {current_sim_team_id} could not make a pick. Ending round early.")
                    done = True 
                    info['draft_ended_prematurely_sim_team'] = True
                    break

                self.current_pick_idx += 1
                self.current_pick_number += 1

        # --- 3. Check for Done Condition ---
        if len(self.teams_rosters[self.agent_team_id]['PLAYERS']) >= self.total_roster_size_per_team:
            done = True 
            info['draft_complete'] = True 
        elif self.current_pick_idx >= len(self.draft_order):
            done = True 
            info['draft_ended_prematurely'] = True 

        # --- 4. Final Reward Calculation if Done ---
        if done:
            if 'draft_complete' in info and self.config.BONUS_FOR_FULL_ROSTER > 0:
                reward += self.config.BONUS_FOR_FULL_ROSTER

            final_projected_points = sum(p.projected_points for p in self.teams_rosters[self.agent_team_id]['PLAYERS'])
            reward += final_projected_points
            info['final_score'] = final_projected_points

        # --- 5. Get Next State ---
        observation = self._get_state()

        return observation, reward, done, False, info


    def _generate_snake_draft_order(self, num_teams, total_picks_per_team):
        """Generates a snake draft order for all picks."""
        draft_order = []
        num_rounds = math.ceil(total_picks_per_team) # Each team gets this many picks
        
        # Ensure sufficient players for the draft if possible.
        # This is a basic check.
        # Total players in dummy CSV: 20
        # Total picks requested for full draft (10 teams * 17 players/team) = 170
        # num_rounds will be 2 (floor(20/10)). So 20 total picks.
        if num_rounds * num_teams > len(self.all_players_data):
            num_rounds = math.floor(len(self.all_players_data) / num_teams)
            print(f"Warning: Not enough players for full draft. Reducing to {num_rounds} rounds ({num_rounds * num_teams} total picks).")
            # Ensure at least one round if possible
            if num_rounds == 0 and len(self.all_players_data) > 0:
                num_rounds = 1
                print("Adjusted to at least 1 round as players are available.")
            elif num_rounds == 0:
                 print("No players available to draft. Draft will be empty.")
                 return [] # Return empty draft order if no players

        for round_num in range(num_rounds):
            if round_num % 2 == 0:
                # Odd rounds (1, 3, 5...) - ascending order
                for team_id in range(1, num_teams + 1):
                    draft_order.append(team_id)
            else:
                # Even rounds (2, 4, 6...) - descending order
                for team_id in range(num_teams, 0, -1):
                    draft_order.append(team_id)
        return draft_order

    def _get_info(self):
        """Returns a dictionary with useful information about the current state."""
        return {
            'current_pick_number': self.current_pick_number,
            'current_team_picking': self.draft_order[self.current_pick_idx] if self.current_pick_idx < len(self.draft_order) else None,
            'agent_roster_size': len(self.teams_rosters[self.agent_team_id]['PLAYERS']),
            'available_players_count': len(self.available_players_ids)
        }

    def _get_state(self) -> np.ndarray:
        """
        Constructs the state vector based on enabled features and normalizes it.
        """
        state_values = []
        for feature_name in self.config.ENABLED_STATE_FEATURES:
            if feature_name in self.state_features_map:
                value = self.state_features_map[feature_name]()
                state_values.append(value)
            else:
                print(f"Warning: Unknown state feature '{feature_name}' requested. Appending 0.")
                state_values.append(0)

        state_array = np.array(state_values, dtype=np.float32)

        # Normalize the state array
        if self.config.STATE_NORMALIZATION_METHOD == 'min_max':
            state_array = self._normalize_min_max(state_array)
        elif self.config.STATE_NORMALIZATION_METHOD == 'z_score':
            state_array = self._normalize_z_score(state_array)
        # 'none' means no normalization

        return state_array

    def _normalize_min_max(self, state_array: np.ndarray) -> np.ndarray:
        max_values = {
            "best_available_qb_points": 400.0, "best_available_rb_points": 350.0,
            "best_available_wr_points": 350.0, "best_available_te_points": 300.0,
            "current_roster_qb_count": self.config.ROSTER_STRUCTURE['QB'] + self.config.BENCH_MAXES['QB'],
            "current_roster_rb_count": self.config.ROSTER_STRUCTURE['RB'] + self.config.BENCH_MAXES['RB'],
            "current_roster_wr_count": self.config.ROSTER_STRUCTURE['WR'] + self.config.BENCH_MAXES['WR'],
            "current_roster_te_count": self.config.ROSTER_STRUCTURE['TE'] + self.config.BENCH_MAXES['TE'],
            "available_roster_slots_qb": self.config.ROSTER_STRUCTURE['QB'] + self.config.BENCH_MAXES['QB'],
            "available_roster_slots_rb": self.config.ROSTER_STRUCTURE['RB'] + self.config.BENCH_MAXES['RB'],
            "available_roster_slots_wr": self.config.ROSTER_STRUCTURE['WR'] + self.config.BENCH_MAXES['WR'],
            "available_roster_slots_te": self.config.ROSTER_STRUCTURE['TE'] + self.config.BENCH_MAXES['TE'],
            "available_roster_slots_flex": self.config.ROSTER_STRUCTURE['FLEX'],
            "qb_available_flag": 1.0, "rb_available_flag": 1.0,
            "wr_available_flag": 1.0, "te_available_flag": 1.0,
            "current_pick_number": len(self.draft_order) if self.draft_order else 1.0, # Max possible pick number
            "agent_start_position": float(self.config.NUM_TEAMS), # Max team number
        }
        min_values = {k: 0.0 for k in max_values}
        
        normalized_state = []
        for i, feature_name in enumerate(self.config.ENABLED_STATE_FEATURES):
            val = state_array[i]
            max_val = max_values.get(feature_name)
            min_val = min_values.get(feature_name)

            if max_val is None or min_val is None:
                # Fallback for features not explicitly in max_values, treat as boolean/flag if reasonable
                if 'flag' in feature_name:
                    max_val = 1.0
                    min_val = 0.0
                else: # Generic fallback for unknown numerics
                    print(f"Warning: No explicit min/max for '{feature_name}'. Using 0-1 range if value is 0 or 1, else using value as is.")
                    if val == 0.0 or val == 1.0: # Assume it's a flag if its value is 0 or 1 and no specific range
                        max_val = 1.0
                        min_val = 0.0
                    else: # If it's a number outside 0/1 and no range, return as is (no norm)
                        normalized_state.append(val)
                        continue # Skip to next feature

            if max_val == min_val:
                normalized_state.append(0.0)
            else:
                normalized_state.append((val - min_val) / (max_val - min_val))
        return np.array(normalized_state, dtype=np.float32)

    def _normalize_z_score(self, state_array: np.ndarray) -> np.ndarray:
        # Placeholder for z-score. For proper implementation, you'd collect
        # mean and std deviation over many episodes.
        mean = np.zeros_like(state_array)
        std = np.ones_like(state_array)
        # Avoid division by zero
        std[std == 0] = 1.0 # If std is 0, set to 1 to avoid error. Will result in 0 after subtraction.
        return (state_array - mean) / std

    def _get_best_available_player_by_pos(self, position: str) -> Optional[Player]:
        """Returns the available player with the highest projected points for a given position."""
        best_player = None
        highest_points = -1

        for player_id in self.available_players_ids:
            player = self.player_map[player_id]
            if player.position == position:
                if player.projected_points > highest_points:
                    highest_points = player.projected_points
                    best_player = player
        return best_player

    def _try_select_player_for_team(self, team_id: int, position_choice: str, available_players_ids: set) -> Tuple[bool, Optional[Player]]:
        """
        Attempts to find and 'draft' the best player for a given position and team.
        Returns (True, player) if successful, (False, None) otherwise.
        Handles roster limits and player availability.
        """
        # 1. Check if any players are available for the chosen position
        available_players_for_pos = [
            self.player_map[pid] for pid in available_players_ids
            if self.player_map[pid].position == position_choice
        ]
        if not available_players_for_pos:
            return False, None # No players of this position available at all

        # 2. Check roster limits for the chosen position
        current_pos_count = self.teams_rosters[team_id][position_choice]
        current_flex_count = self.teams_rosters[team_id]['FLEX']

        max_pos_count = self.config.ROSTER_STRUCTURE.get(position_choice, 0) + self.config.BENCH_MAXES.get(position_choice, 0)
        max_flex_count = self.config.ROSTER_STRUCTURE.get('FLEX', 0)

        # A player can fill:
        # a) a direct starter spot for their position
        # b) a bench spot for their position
        # c) a FLEX spot (if RB/WR/TE) AND if primary + bench spots are full AND FLEX spots are available

        has_room = False
        if current_pos_count < self.config.ROSTER_STRUCTURE.get(position_choice, 0):
            # Room in starting roster
            has_room = True
        elif current_pos_count < max_pos_count:
            # Room in bench spots for this specific position
            has_room = True
        elif position_choice in ['RB', 'WR', 'TE'] and current_flex_count < max_flex_count:
            # Room in FLEX spot
            has_room = True

        if not has_room:
            return False, None # No room for this specific position, nor a FLEX spot if applicable

        # Find the best available player for the chosen position (highest projected points)
        best_player = None
        max_points = -1
        for p in available_players_for_pos:
            if p.projected_points > max_points:
                max_points = p.projected_points
                best_player = p

        if best_player:
            return True, best_player
        else:
            return False, None # Should not happen if available_players_for_pos is not empty and has_room is True

    def _update_roster_counts(self, team_id: int, player: Player):
        """Updates team's roster counts, including handling FLEX logic."""
        
        # Check if the player fills a direct position spot first
        current_pos_count = self.teams_rosters[team_id][player.position]
        required_fixed_spots = self.config.ROSTER_STRUCTURE.get(player.position, 0)
        max_pos_spots = required_fixed_spots + self.config.BENCH_MAXES.get(player.position, 0)

        if current_pos_count < max_pos_spots:
            # Player fills a direct position spot (starter or bench)
            self.teams_rosters[team_id][player.position] += 1
        elif player.position in ['RB', 'WR', 'TE'] and self.teams_rosters[team_id]['FLEX'] < self.config.ROSTER_STRUCTURE['FLEX']:
            # Player fills a FLEX spot
            self.teams_rosters[team_id]['FLEX'] += 1
        else:
            # This case indicates an error in _try_select_player_for_team or environment logic
            # as a player should only be "drafted" if there's a spot.
            print(f"Error: Player {player.name} ({player.position}) drafted for team {team_id} but no valid spot found to update counts!")
            # This shouldn't be reached if _try_select_player_for_team works correctly.

    def _simulate_competing_pick(self, team_id: int) -> Optional[Player]:
        roster_counts = self.teams_rosters[team_id]
        
        # Define positions that our environment and roster structure can handle.
        # This includes all positions defined in ROSTER_STRUCTURE (excluding FLEX as it's an action)
        # plus positions that can fill FLEX.
        handled_positions = set(self.config.ROSTER_STRUCTURE.keys()) - {'FLEX'} # e.g., {'QB', 'RB', 'WR', 'TE'}
        flex_eligible_positions = {'RB', 'WR', 'TE'} # Positions that can fill FLEX spots
        all_draftable_positions = handled_positions.union(flex_eligible_positions)

        # Filter the available players to only include those with positions our logic can handle.
        available_players_filtered = [self.player_map[pid] 
                                      for pid in self.available_players_ids 
                                      if self.player_map[pid].position in all_draftable_positions]

        if self.config.COMPETING_TEAM_LOGIC == 'ADP':
            eligible_players_for_pick = []
            for player in available_players_filtered: # Iterate only over filtered players
                pos = player.position
                current_pos_count = roster_counts[pos]
                current_flex_count = roster_counts['FLEX']

                max_pos_count = self.config.ROSTER_STRUCTURE.get(pos, 0) + self.config.BENCH_MAXES.get(pos, 0)
                max_flex_count = self.config.ROSTER_STRUCTURE.get('FLEX', 0)

                has_room = False
                if current_pos_count < max_pos_count:
                    has_room = True # Room in specific position's starter or bench slots
                elif pos in ['RB', 'WR', 'TE'] and current_flex_count < max_flex_count:
                    has_room = True # Room in a FLEX spot
                
                if has_room:
                    eligible_players_for_pick.append(player)
            
            if eligible_players_for_pick:
                # Pick the best ADP player among all eligible options that fit any slot
                eligible_players_for_pick.sort(key=lambda p: p.adp)
                return eligible_players_for_pick[0]
            else:
                return None # No eligible player found for this team

        elif self.config.COMPETING_TEAM_LOGIC == 'HEURISTIC':
            # Heuristic strategy:
            # Prioritize filling starting spots first, then bench, then flex,
            # picking the highest projected player for that specific available spot.

            # 1. Attempt to fill core starting spots (QB, RB, WR, TE)
            for pos in ['QB', 'RB', 'WR', 'TE']: # Order of priority can be configured
                if roster_counts[pos] < self.config.ROSTER_STRUCTURE.get(pos, 0):
                    eligible_players_for_pos = sorted([p for p in available_players_filtered if p.position == pos], key=lambda p: p.projected_points, reverse=True)
                    if eligible_players_for_pos:
                        return eligible_players_for_pos[0] # Pick best available for starter spot

            # 2. Then try to fill bench spots (QB, RB, WR, TE)
            for pos in ['RB', 'WR', 'TE', 'QB']: # Prioritize valuable bench positions
                max_pos_count_total = self.config.ROSTER_STRUCTURE.get(pos, 0) + self.config.BENCH_MAXES.get(pos, 0)
                if roster_counts[pos] < max_pos_count_total:
                    eligible_players_for_pos = sorted([p for p in available_players_filtered if p.position == pos], key=lambda p: p.projected_points, reverse=True)
                    if eligible_players_for_pos:
                        return eligible_players_for_pos[0] # Pick best available for bench spot

            # 3. Finally, try to fill FLEX spots if available, with best RB/WR/TE
            if roster_counts['FLEX'] < self.config.ROSTER_STRUCTURE['FLEX']:
                 eligible_flex_players = sorted([p for p in available_players_filtered if p.position in ['RB', 'WR', 'TE']], key=lambda p: p.projected_points, reverse=True)
                 if eligible_flex_players:
                     return eligible_flex_players[0] # Pick best available for FLEX
            
            return None # No viable pick found with heuristic for this team

        else:
            raise ValueError(f"Unknown competing team logic: {self.config.COMPETING_TEAM_LOGIC}")

    def render(self):
        """
        Renders the current state of the environment.
        For fantasy football, this could print current draft board, agent's roster, etc.
        """
        current_team_picking_id = self.draft_order[self.current_pick_idx] if self.current_pick_idx < len(self.draft_order) else 'N/A'
        print(f"\n--- Current Pick: {self.current_pick_number} (Team {current_team_picking_id}) ---")
        
        # Display agent's roster
        agent_roster_data = self.teams_rosters[self.agent_team_id]
        print(f"Agent's Roster (Team {self.agent_team_id}):")
        roster_summary = []
        for pos_type, count_needed in self.config.ROSTER_STRUCTURE.items():
            if pos_type == 'FLEX':
                roster_summary.append(f"{pos_type}: {agent_roster_data['FLEX']}/{count_needed}")
            else:
                bench_max = self.config.BENCH_MAXES.get(pos_type, 0)
                current_total = agent_roster_data[pos_type]
                roster_summary.append(f"{pos_type}: {current_total}/{count_needed + bench_max}")
        print("  " + " | ".join(roster_summary))
        
        # Optionally, list players
        # print("  Players:", [p.name for p in agent_roster_data['PLAYERS']])

        # Display top available players by position (optional)
        print("\nTop Available Players (by Projected Points):")
        for pos in ['QB', 'RB', 'WR', 'TE']:
            best_player = self._get_best_available_player_by_pos(pos)
            if best_player:
                print(f"  {pos}: {best_player.name} ({best_player.projected_points:.1f} pts, ADP {best_player.adp:.1f})")
            else:
                print(f"  {pos}: None available")

    def close(self):
        """Clean up resources."""
        pass
</file>

<file path="policy_network.py">
import torch
import torch.nn as nn
import torch.nn.functional as F
from torch.distributions import Categorical

class PolicyNetwork(nn.Module):
    """
    Neural network that represents the agent's policy.
    Takes the environment state as input and outputs action probabilities.
    """
    def __init__(self, input_dim: int, output_dim: int, hidden_dim: int = 64):
        """
        Initializes the Policy Network.

        Args:
            input_dim (int): Dimensionality of the input state.
            output_dim (int): Dimensionality of the output action space (number of possible actions).
            hidden_dim (int): Number of neurons in the hidden layers.
        """
        super(PolicyNetwork, self).__init__()

        # Define the layers of the neural network
        # Input Layer -> Hidden Layer 1
        self.fc1 = nn.Linear(input_dim, hidden_dim)
        # Hidden Layer 1 -> Hidden Layer 2
        self.fc2 = nn.Linear(hidden_dim, hidden_dim)
        # Hidden Layer 2 -> Output Layer (logits for action probabilities)
        self.fc3 = nn.Linear(hidden_dim, output_dim)

    def forward(self, state: torch.Tensor) -> torch.Tensor:
        """
        Performs a forward pass through the network.

        Args:
            state (torch.Tensor): The current state observation from the environment.

        Returns:
            torch.Tensor: The raw output (logits) for each action.
        """
        # Apply ReLU activation function after each hidden layer
        x = F.relu(self.fc1(state))
        x = F.relu(self.fc2(x))
        # The output layer produces logits, which will be converted to probabilities
        # later using softmax. No activation here as softmax is applied externally.
        logits = self.fc3(x)
        return logits

    def get_action_probabilities(self, state: torch.Tensor) -> torch.Tensor:
        """
        Computes the action probabilities from the network's output.

        Args:
            state (torch.Tensor): The current state observation.

        Returns:
            torch.Tensor: A tensor of probabilities for each action.
        """
        logits = self.forward(state)
        # Apply softmax to convert logits into probabilities
        action_probs = F.softmax(logits, dim=-1)
        return action_probs

    def sample_action(self, state: torch.Tensor) -> tuple[int, torch.Tensor]:
        """
        Samples an action from the policy distribution and returns its log probability.

        Args:
            state (torch.Tensor): The current state observation.

        Returns:
            tuple[int, torch.Tensor]: A tuple containing the sampled action (int)
                                      and its log probability (torch.Tensor).
        """
        # Ensure state is a float tensor and has a batch dimension (even if batch size is 1)
        state = state.float().unsqueeze(0) # Add batch dimension

        action_probs = self.get_action_probabilities(state)

        # Create a categorical distribution from the action probabilities
        m = Categorical(action_probs)

        # Sample an action from the distribution
        action = m.sample()

        # Get the log probability of the sampled action
        log_prob = m.log_prob(action)

        return action.item(), log_prob
</file>

<file path="reinforce_agent.py">
import torch
import torch.optim as optim
import numpy as np
from typing import List, Tuple

from policy_network import PolicyNetwork
from fantasy_draft_env import FantasyFootballDraftEnv # Import our environment
from config import Config # Import our configuration

class ReinforceAgent:
    """
    Implements the REINFORCE (Monte Carlo Policy Gradient) algorithm.
    """
    def __init__(self, env: FantasyFootballDraftEnv, config: Config):
        """
        Initializes the REINFORCE Agent.

        Args:
            env (FantasyFootballDraftEnv): The OpenAI Gym environment.
            config (Config): The project configuration object.
        """
        self.env = env
        self.config = config

        # Determine input and output dimensions for the policy network
        input_dim = len(config.ENABLED_STATE_FEATURES)
        output_dim = env.action_space.n # Number of discrete actions (QB, RB, WR, TE)

        # Initialize the Policy Network
        self.policy_network = PolicyNetwork(input_dim, output_dim, config.HIDDEN_DIM)
        # Initialize the optimizer
        self.optimizer = optim.Adam(self.policy_network.parameters(), lr=config.LEARNING_RATE)

        # Store rewards and log probabilities for each episode
        self.episode_log_probs: List[torch.Tensor] = []
        self.episode_rewards: List[float] = [] # Raw rewards received at each step

    def _calculate_returns(self, rewards: List[float]) -> List[float]:
        """
        Calculates the discounted returns (G_t) for an episode.
        G_t = r_t + gamma * r_{t+1} + gamma^2 * r_{t+2} + ... + gamma^(T-t) * r_T
        """
        returns: List[float] = []
        G = 0.0
        # Iterate backwards through the rewards to calculate discounted sum
        for r in reversed(rewards):
            G = r + self.config.DISCOUNT_FACTOR * G
            returns.insert(0, G) # Insert at the beginning to keep original order
        return returns

    def train(self) -> Tuple[List[float], List[float]]:
        """
        Runs the REINFORCE training loop for a specified number of episodes.

        Returns:
            Tuple[List[float], List[float]]: Lists of total rewards per episode and policy losses per episode.
        """
        all_episode_rewards = []
        all_policy_losses = []

        print(f"Starting REINFORCE training for {self.config.TOTAL_EPISODES} episodes...")
        print(f"State features enabled: {self.config.ENABLED_STATE_FEATURES}")
        print(f"Learning Rate: {self.config.LEARNING_RATE}, Discount Factor: {self.config.DISCOUNT_FACTOR}")

        for episode in range(1, self.config.TOTAL_EPISODES + 1):
            state, info = self.env.reset()
            self.episode_log_probs = []
            self.episode_rewards = []
            episode_done = False
            total_episode_reward = 0

            # --- 1. Rollout an Episode ---
            while not episode_done:
                # Convert numpy state to torch tensor
                state_tensor = torch.from_numpy(state).float()

                # Sample an action from the policy
                action, log_prob = self.policy_network.sample_action(state_tensor)

                # Take the action in the environment
                next_state, reward, done, truncated, info = self.env.step(action)

                # Store log probability and reward
                self.episode_log_probs.append(log_prob)
                self.episode_rewards.append(reward)
                total_episode_reward += reward

                state = next_state
                episode_done = done or truncated # Consider truncated also as episode end

            # --- 2. Calculate Returns ---
            returns = self._calculate_returns(self.episode_rewards)
            returns_tensor = torch.tensor(returns, dtype=torch.float32)

            # Normalize returns (standard practice for stability in REINFORCE)
            # Avoid division by zero if all returns are the same
            if len(returns_tensor) > 1 and returns_tensor.std() > 1e-6:
                returns_tensor = (returns_tensor - returns_tensor.mean()) / (returns_tensor.std() + 1e-8)
            else:
                # If only one or no returns, or std is zero, no normalization needed or possible
                pass

            # --- 3. Compute Policy Loss ---
            # Loss = - sum(log_prob * G_t)
            policy_loss = []
            for log_prob, G_t in zip(self.episode_log_probs, returns_tensor):
                policy_loss.append(-log_prob * G_t) # Negative sign for gradient ascent (maximize reward)

            # Combine all step losses for the episode
            policy_loss = torch.stack(policy_loss).sum()

            # --- 4. Perform Optimization Step ---
            self.optimizer.zero_grad() # Clear gradients from previous step
            policy_loss.backward()      # Compute gradients
            self.optimizer.step()       # Update network weights

            # --- 5. Logging and Reporting ---
            all_episode_rewards.append(total_episode_reward)
            all_policy_losses.append(policy_loss.item())

            if episode % 100 == 0:
                print(f"Episode {episode}/{self.config.TOTAL_EPISODES} | "
                      f"Total Reward: {total_episode_reward:.2f} | "
                      f"Policy Loss: {policy_loss.item():.4f} | "
                      f"Agent Roster Size: {len(self.env.teams_rosters[self.env.agent_team_id]['PLAYERS'])} "
                      f"(Final Score: {info.get('final_score', 'N/A')})"
                     )
                # self.env.render() # Optional: Render environment state periodically

        print("\nTraining complete!")
        return all_episode_rewards, all_policy_losses
</file>

<file path="requirements.txt">
pandas
numpy
torch
gym
matplotlib
</file>

<file path="run.sh">
#!/bin/bash

# Define your Docker image name
IMAGE_NAME="fantasy-football-ai"
TAG="latest"

# Define the command to run inside the container
# NOW we execute our main training script!
# COMMAND="python train.py"
COMMAND="python simulate.py"

echo "Running Docker container for ${IMAGE_NAME}:${TAG}"

# Check if the data, models, and logs directories exist locally
# If not, create them to ensure consistent volume mounts
mkdir -p data models logs

# Run the Docker container
docker run \
    -it \
    --rm \
    -v "$(pwd)/data:/app/data" \
    -v "$(pwd)/models:/app/models" \
    -v "$(pwd)/logs:/app/logs" \
    "${IMAGE_NAME}:${TAG}" \
    /bin/bash -c "${COMMAND}"

if [ $? -eq 0 ]; then
    echo "Container finished successfully."
else
    echo "Container exited with an error."
    exit 1
fi
</file>

<file path="simulate.py">
import torch
import os
import datetime
from collections import defaultdict
from typing import List, Dict, Optional
import numpy as np

from config import Config
from fantasy_draft_env import FantasyFootballDraftEnv
from policy_network import PolicyNetwork
from data_utils import Player

def calculate_roster_scores(team_roster: List[Player], roster_structure: Dict, bench_maxes: Dict) -> Dict:
    """
    Calculates projected points for starters, bench, and combined based on a team's drafted players.
    This logic attempts to fill starting positions first, then bench.
    FLEX players are assigned after primary positions are filled.
    """
    starters = defaultdict(list)
    bench = defaultdict(list)
    flex_eligible = [] # Players eligible for FLEX if primary spots are full

    # Sort players by projected points (descending) for optimal placement
    sorted_players = sorted(team_roster, key=lambda p: p.projected_points, reverse=True)

    temp_pos_counts = defaultdict(int) # Tracks players in specific position slots (QB, RB, WR, TE)
    current_flex_fill = 0 # Tracks players filling FLEX slots

    for player in sorted_players:
        pos = player.position
        
        # 1. Try to fill a primary starting spot
        if temp_pos_counts[pos] < roster_structure.get(pos, 0):
            starters[pos].append(player)
            temp_pos_counts[pos] += 1
        # 2. Try to fill a primary bench spot
        elif temp_pos_counts[pos] < (roster_structure.get(pos, 0) + bench_maxes.get(pos, 0)):
            bench[pos].append(player)
            temp_pos_counts[pos] += 1
        # 3. If RB/WR/TE and primary/bench spots are full, try for FLEX
        elif pos in ['RB', 'WR', 'TE'] and current_flex_fill < roster_structure.get('FLEX', 0):
            flex_eligible.append(player)
            current_flex_fill += 1
        else:
            # This player couldn't be optimally assigned. This can happen if, for example,
            # a team drafts too many players of a certain position beyond all their starter,
            # bench, AND flex needs. For scoring, we'll just put them on the bench.
            # This indicates the agent might be drafting sub-optimally or roster size is complex.
            bench[pos].append(player) # Fallback to bench
            print(f"Warning: Player {player.name} ({player.position}) could not be optimally assigned to a starter/flex slot. Placed on bench.")


    # Calculate total points for each segment
    starter_points = sum(p.projected_points for pos_list in starters.values() for p in pos_list)
    bench_points = sum(p.projected_points for pos_list in bench.values() for p in pos_list)
    flex_points = sum(p.projected_points for p in flex_eligible)
    
    combined_total_points = starter_points + bench_points + flex_points
    
    # Calculate average points
    num_starters = sum(len(lst) for lst in starters.values()) + len(flex_eligible)
    num_bench = sum(len(lst) for lst in bench.values())

    avg_starter_points = starter_points / num_starters if num_starters > 0 else 0
    avg_bench_points = bench_points / num_bench if num_bench > 0 else 0
    avg_combined_points = combined_total_points / (num_starters + num_bench) if (num_starters + num_bench) > 0 else 0

    return {
        'starters_total_points': starter_points,
        'bench_total_points': bench_points,
        'flex_total_points': flex_points,
        'combined_total_points': combined_total_points,
        'starters_avg_points': avg_starter_points,
        'bench_avg_points': avg_bench_points,
        'combined_avg_points': avg_combined_points,
        'roster_size': len(team_roster)
    }


def simulate_drafts(config: Config, num_runs: int):
    """
    Loads a trained agent and simulates multiple fantasy football drafts, logging all picks.
    """
    print(f"\n--- Starting {num_runs} Draft Simulations ---")
    
    # 1. Load the trained Policy Network
    # Need to know the input_dim and output_dim, which are from the environment/config
    # Temporarily instantiate env to get action space dimensions safely
    temp_env_for_dims = FantasyFootballDraftEnv(config)
    input_dim = len(config.ENABLED_STATE_FEATURES)
    output_dim = len(temp_env_for_dims.action_to_position) 
    
    policy_network = PolicyNetwork(input_dim, output_dim, config.HIDDEN_DIM)
    
    try:
        policy_network.load_state_dict(torch.load(config.MODEL_PATH_TO_LOAD))
        policy_network.eval() # Set to evaluation mode (e.g., disables dropout layers)
        print(f"Successfully loaded model from: {config.MODEL_PATH_TO_LOAD}")
    except FileNotFoundError:
        print(f"Error: Model not found at {config.MODEL_PATH_TO_LOAD}. Please ensure the path is correct.")
        print("Aborting simulation.")
        return
    except Exception as e:
        print(f"Error loading model: {e}")
        print("Aborting simulation.")
        return

    # 2. Prepare Environment for Simulation
    env = FantasyFootballDraftEnv(config) # Main environment instance

    # This loop will now run pick-by-pick directly, not using env.step's internal opponent simulation
    all_simulation_results = []

    for run_idx in range(num_runs):
        print(f"\n--- Simulation Run {run_idx + 1}/{num_runs} ---")
        
        # Reset environment for a new draft. This also pre-simulates up to agent's first pick.
        # We need the state *after* the initial reset, to be ready for the agent's pick.
        state, info = env.reset() 
        
        # Ensure the draft log for this run starts with any picks made during env.reset() advance
        # Note: env.reset() prints warnings about competing team failing to pick if needed.
        
        # We need a way to get the full log of picks *made so far* by env.reset()
        # The current env.reset() does not expose this cleanly.
        # To get the full draft log, we'll manually simulate pick by pick here instead
        # of relying on env.reset() to advance. This is more explicit for logging.

        # Re-reset and manage all picks in simulate.py for comprehensive logging
        env.available_players_ids = {p.player_id for p in env.all_players_data}
        env.teams_rosters = defaultdict(lambda: {'QB': 0, 'RB': 0, 'WR': 0, 'TE': 0, 'FLEX': 0, 'PLAYERS': []})
        env.current_pick_idx = 0
        env.current_pick_number = 1 
        
        draft_order_for_run = env._generate_snake_draft_order(env.config.NUM_TEAMS, env.total_roster_size_per_team)
        # Ensure draft_order_for_run is set for this simulation
        env.draft_order = draft_order_for_run # Update env's internal draft order
        
        draft_log = [] # To store details of each pick in this simulation
        
        done = False
        while not done:
            if env.current_pick_idx >= len(env.draft_order):
                done = True # All picks in the planned draft order are exhausted
                break
            
            current_team_id = env.draft_order[env.current_pick_idx]
            player_picked = None
            action_taken = None # Store action for agent picks

            if current_team_id == env.agent_team_id:
                # Agent's turn: Use trained policy
                state_for_agent = env._get_state() # Get the current state for the agent
                state_tensor = torch.from_numpy(state_for_agent).float().unsqueeze(0)
                with torch.no_grad():
                    action_probs = policy_network.get_action_probabilities(state_tensor)
                    action_taken = torch.argmax(action_probs).item() # Greedy pick

                is_valid, drafted_player_obj = env._try_select_player_for_team(
                    current_team_id, env.action_to_position[action_taken], env.available_players_ids
                )
                if is_valid:
                    player_picked = drafted_player_obj
                else:
                    # Agent made an invalid pick. In simulation, we just log this and move on.
                    # It doesn't get a penalty here, and the episode continues to fill up the roster.
                    print(f"  Sim Warning: Agent (Team {current_team_id}) made an invalid pick at Pick {env.current_pick_number}. Attempted {env.action_to_position[action_taken]}.")
                    # No player_picked, so no changes to rosters/available_players for this turn.

            else:
                # Competing team's turn: Use their configured logic
                player_picked = env._simulate_competing_pick(current_team_id)
                if not player_picked:
                    # If a competing team can't make a valid pick (e.g., no players left to fit needs)
                    print(f"  Sim Warning: Competing team {current_team_id} could not make a pick at Pick {env.current_pick_number}.")
                    # No player_picked, so no changes to rosters/available_players for this turn.

            # --- Log and Update Environment for the current pick ---
            if player_picked:
                draft_log.append({
                    'pick_number': env.current_pick_number,
                    'team_id': current_team_id,
                    'player_id': player_picked.player_id,
                    'name': player_picked.name,
                    'position': player_picked.position,
                    'projected_points': player_picked.projected_points,
                    'adp': player_picked.adp,
                    'status': 'Valid Pick'
                })
                # Update environment state manually for this single pick
                env.teams_rosters[current_team_id]['PLAYERS'].append(player_picked)
                env.available_players_ids.remove(player_picked.player_id)
                env._update_roster_counts(current_team_id, player_picked)
            else:
                # Log a skipped pick (due to invalid action or no valid player found)
                draft_log.append({
                    'pick_number': env.current_pick_number,
                    'team_id': current_team_id,
                    'player_id': None,
                    'name': 'NO_PICK',
                    'position': 'N/A',
                    'projected_points': 0.0,
                    'adp': 0.0,
                    'status': 'Skipped/Invalid Pick'
                })

            # Advance to the next pick in the draft order
            env.current_pick_idx += 1
            env.current_pick_number += 1

            # Check for done condition:
            # The draft is done if all picks in the order are exhausted OR
            # all teams have filled their rosters OR no players are left
            all_rosters_full = True
            for team_id_check in range(1, config.NUM_TEAMS + 1):
                if len(env.teams_rosters[team_id_check]['PLAYERS']) < env.total_roster_size_per_team:
                    all_rosters_full = False
                    break
            
            if all_rosters_full or env.current_pick_idx >= len(env.draft_order) or not env.available_players_ids:
                done = True
                
        # --- End of Simulation Run ---
        run_results = {
            'draft_log': draft_log,
            'final_team_rosters': {team_id: env.teams_rosters[team_id]['PLAYERS'] for team_id in range(1, config.NUM_TEAMS + 1)}
        }
        all_simulation_results.append(run_results)

    # --- Print Summary of Simulations ---
    print("\n--- Simulation Summary ---")
    avg_agent_scores = []
    
    # Store individual team scores for average across runs
    all_runs_team_scores = defaultdict(lambda: defaultdict(float)) # team_id -> {total, starters, bench, flex}

    for run_idx, result in enumerate(all_simulation_results):
        print(f"\nRun {run_idx + 1}:")
        
        # Display draft log
        print("\n  Draft Log:")
        for pick in result['draft_log']:
            # Ensure 'name' is handled for 'NO_PICK' scenarios
            player_name_display = pick['name'] if pick['name'] != 'NO_PICK' else pick['status']
            print(f"    Pick {pick['pick_number']:<3} | Team {pick['team_id']:<2} | {pick['position']:<3} {player_name_display:<25} | {pick['projected_points']:.1f} pts | ADP {pick['adp']:.1f} | {pick['status']}")

        # Calculate and display final team scores
        print("\n  Final Team Scores:")
        for team_id, players in result['final_team_rosters'].items():
            scores = calculate_roster_scores(players, config.ROSTER_STRUCTURE, config.BENCH_MAXES)
            print(f"    Team {team_id}: Roster Size={scores['roster_size']}/{env.total_roster_size_per_team} | Total Combined={scores['combined_total_points']:.1f} | Avg Combined={scores['combined_avg_points']:.1f}")
            print(f"      Starters: {scores['starters_total_points']:.1f} total, {scores['starters_avg_points']:.1f} avg")
            print(f"      Bench:    {scores['bench_total_points']:.1f} total, {scores['bench_avg_points']:.1f} avg")
            print(f"      FLEX:     {scores['flex_total_points']:.1f} total") # Display FLEX points explicitly

            # Aggregate scores for overall averages
            all_runs_team_scores[team_id]['combined_total'] += scores['combined_total_points']
            all_runs_team_scores[team_id]['starters_total'] += scores['starters_total_points']
            all_runs_team_scores[team_id]['bench_total'] += scores['bench_total_points']
            all_runs_team_scores[team_id]['flex_total'] += scores['flex_total_points']
            all_runs_team_scores[team_id]['roster_size_total'] += scores['roster_size']
            all_runs_team_scores[team_id]['count'] += 1

    print("\n--- Overall Averages Across All Runs ---")
    overall_avg_agent_score = 0
    overall_avg_opponent_score = 0
    
    for team_id in sorted(all_runs_team_scores.keys()):
        team_data = all_runs_team_scores[team_id]
        runs_count = team_data['count']
        
        avg_combined = team_data['combined_total'] / runs_count
        avg_starters = team_data['starters_total'] / runs_count
        avg_bench = team_data['bench_total'] / runs_count
        avg_flex = team_data['flex_total'] / runs_count
        avg_roster_size = team_data['roster_size_total'] / runs_count

        if team_id == config.AGENT_START_POSITION:
            overall_avg_agent_score = avg_combined
            print(f"Agent (Team {team_id}):")
            print(f"  Avg Roster Size: {avg_roster_size:.1f}/{env.total_roster_size_per_team}")
            print(f"  Avg Total Combined Points: {avg_combined:.1f}")
            print(f"  Avg Starters Points: {avg_starters:.1f}")
            print(f"  Avg Bench Points: {avg_bench:.1f}")
            print(f"  Avg FLEX Points: {avg_flex:.1f}")
        else:
            overall_avg_opponent_score += avg_combined
            print(f"Opponent Team {team_id}:")
            print(f"  Avg Roster Size: {avg_roster_size:.1f}/{env.total_roster_size_per_team}")
            print(f"  Avg Total Combined Points: {avg_combined:.1f}")
            print(f"  Avg Starters Points: {avg_starters:.1f}")
            print(f"  Avg Bench Points: {avg_bench:.1f}")
            print(f"  Avg FLEX Points: {avg_flex:.1f}")

    if config.NUM_TEAMS > 1: # Only calculate average opponent if there are opponents
        overall_avg_opponent_score /= (config.NUM_TEAMS - 1)
        print(f"\nOverall Average Opponent Total Score (across all opponent teams and runs): {overall_avg_opponent_score:.1f}")
    
    if overall_avg_agent_score > overall_avg_opponent_score:
        print("\nAgent's average score is HIGHER than opponents' average score. Good job!")
    elif overall_avg_agent_score < overall_avg_opponent_score:
        print("\nAgent's average score is LOWER than opponents' average score. Keep training!")
    else:
        print("\nAgent's average score is EQUAL to opponents' average score.")


if __name__ == "__main__":
    config = Config()
    simulate_drafts(config, config.NUM_SIMULATION_RUNS)
</file>

<file path="train.py">
import torch
import os
import matplotlib.pyplot as plt
import numpy as np
import datetime

from config import Config
from fantasy_draft_env import FantasyFootballDraftEnv
from reinforce_agent import ReinforceAgent
from policy_network import PolicyNetwork # Not directly instantiated here, but useful for context if loading a model

def main():
    """
    Main function to initialize the environment, agent, and run the training process.
    """
    print("--- Starting Fantasy Football Draft AI Training ---")

    # 1. Load Configuration
    config = Config()
    print(f"Project Base Directory: {config.BASE_DIR}")
    print(f"Data CSV: {config.PLAYER_DATA_CSV}")
    print(f"Total Episodes: {config.TOTAL_EPISODES}")
    print(f"Agent Start Position: {config.AGENT_START_POSITION}")
    print(f"State Normalization: {config.STATE_NORMALIZATION_METHOD}")
    print(f"Intermediate Reward Enabled: {config.ENABLE_INTERMEDIATE_REWARD}")

    # 2. Initialize Environment
    print("\nInitializing Fantasy Football Draft Environment...")
    env = FantasyFootballDraftEnv(config)
    print(f"Observation Space Dimension: {env.observation_space_dim}")
    print(f"Action Space: {env.action_space.n} actions ({env.action_to_position})")

    # 3. Initialize Agent
    print("\nInitializing REINFORCE Agent...")
    agent = ReinforceAgent(env, config)
    print(f"Policy Network Architecture: Input({len(config.ENABLED_STATE_FEATURES)}) -> Hidden({config.HIDDEN_DIM}) -> Hidden({config.HIDDEN_DIM}) -> Output({env.action_space.n})")
    print(f"Learning Rate: {config.LEARNING_RATE}, Discount Factor (Gamma): {config.DISCOUNT_FACTOR}")

    # 4. Train the Agent
    print("\nStarting Agent Training...")
    episode_rewards, policy_losses = agent.train()
    print("\nAgent training complete!")

    # 5. Save the trained model
    model_save_path = os.path.join(config.MODELS_DIR, f"reinforce_policy_model_{datetime.datetime.now().strftime('%Y%m%d_%H%M%S')}.pth")
    torch.save(agent.policy_network.state_dict(), model_save_path)
    print(f"\nPolicy network model saved to: {model_save_path}")

    # 6. Plotting Training Results
    print("\nGenerating training plots...")
    timestamp = datetime.datetime.now().strftime('%Y%m%d_%H%M%S')
    
    # Plot Total Rewards
    plt.figure(figsize=(12, 6))
    plt.plot(episode_rewards)
    plt.title('Total Reward per Episode')
    plt.xlabel('Episode')
    plt.ylabel('Total Reward')
    plt.grid(True)
    rewards_plot_path = os.path.join(config.LOGS_DIR, f"rewards_plot_{timestamp}.png")
    plt.savefig(rewards_plot_path)
    plt.close()
    print(f"Rewards plot saved to: {rewards_plot_path}")

    # Plot Policy Losses
    plt.figure(figsize=(12, 6))
    plt.plot(policy_losses)
    plt.title('Policy Loss per Episode')
    plt.xlabel('Episode')
    plt.ylabel('Loss')
    plt.grid(True)
    losses_plot_path = os.path.join(config.LOGS_DIR, f"losses_plot_{timestamp}.png")
    plt.savefig(losses_plot_path)
    plt.close()
    print(f"Losses plot saved to: {losses_plot_path}")
    
    print("\nTraining process finished.")
    print("You can inspect the saved model and plots in the 'models/' and 'logs/' directories.")

if __name__ == "__main__":
    main()
</file>

</files>
